---
status: partial
phase: 08.5-temporal-session-analysis
source: [08.5-VERIFICATION.md]
started: 2026-04-27T18:00:00Z
updated: 2026-04-27T18:00:00Z
---

## Current Test

[awaiting human action — not blocking Phase 9 development]

## Tests

### 1. Provision SUPABASE_DB_URL in V2/.env (carry-over from Phase 8.4)
expected: V2/.env contains `SUPABASE_DB_URL=postgresql://postgres.nubmgoyyndtolsjyynln:...@aws-0-<region>.pooler.supabase.com:5432/postgres` (Session pooler URI, port 5432). Smoke-test passes: `python3 -c "from dotenv import load_dotenv; load_dotenv('V2/.env'); import os, psycopg; psycopg.connect(os.environ['SUPABASE_DB_URL'], prepare_threshold=None).cursor().execute('SELECT 1')` returns success.
result: [pending]

### 2. Pre-warm Supabase cache from canonical CSVs
expected: After URL provisioned: `cd V2 && python -m scripts.update_cache --all` inserts ~250k rows (8 pairs × {M15, H1, H4} × ~10k bars). Idempotency check: re-run reports `0 new bars`.
result: [pending]

### 3. Run full-corpus Phase 8.5 pipeline
expected: `cd V2 && python -m scripts.run_temporal_analysis` produces:
- `.planning/phases/08.5-temporal-session-analysis/evidence/session_performance_*.csv` (≥10 files for 19 active combos minus thin AUDNZD subset)
- `.planning/phases/08.5-temporal-session-analysis/evidence/heatmap_*.png` (≥30 files: hour+dow for all combos plus dom+doy for H1+Daily combos)
- `.planning/phases/08.5-temporal-session-analysis/evidence/risk_calendar.yaml` populated with detected recurring patterns
- `V2/v3_intelligence/session_config.py` re-generated with non-empty SESSION_RULES + BLACKOUT_PATTERNS
result: [pending]

### 4. Visual review of heatmaps + risk_calendar.yaml
expected: Operator opens 3-5 heatmaps from evidence/ — verifies diverging colormap centered on 0, gray cells for insufficient_evidence, axis ticks readable. Reviews risk_calendar.yaml — confirms detected patterns look reasonable (FOMC/NFP-shaped windows for USD pairs, BoJ-shaped for JPY pairs, etc.) before letting Phase 9 router consume them.
result: [pending]

### 5. Re-run unit suite to confirm SESSION_RULES populated path
expected: After phase-gate run: `cd V2 && python -m pytest tests/v3_intelligence/test_session_filters.py -v` still 3/3 GREEN; one of the cases now exercises a non-empty SESSION_RULES rule (rather than the empty-config default).
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

(none — all items are non-blocking carry-overs that do not gate Phase 9 development; the typed predicate API is fully importable and behaves correctly on empty configs today)
