# Phase 8.5: Temporal & Session Analysis - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-27
**Phase:** 08.5-temporal-session-analysis
**Mode:** `--auto` (Claude auto-selected recommended defaults at every decision point)
**Areas discussed:** Session windows + timezone, Statistical significance, Filter contract, Risk calendar source

---

## Session windows + timezone

| Option | Description | Selected |
|--------|-------------|----------|
| ROADMAP UTC ranges fixed (no DST) | Tokyo 00-09, London 07-16, NY 13-22, Overlap 13-16, London open 07-09 — exact ROADMAP wording, no DST adjustment because IC Markets cache delivers UTC bars (Phase 8.4 D-02) | ✓ |
| Server-time ranges with DST | Adjust for broker server (typically EET/EEST = UTC+2/+3); requires DST tracking | |
| Floating ranges keyed to NY session DST | Most retail-trader convention but adds complexity for no analytical gain since cache is UTC | |

**User's choice:** ROADMAP UTC ranges fixed (no DST adjustment) — auto-selected per `--auto`.
**Notes:** Cache delivers UTC TIMESTAMPTZ (Phase 8.4 D-02), so server-time conversion is N/A. Recorded as D-01 / D-02 in CONTEXT.md.

---

| Option | Description | Selected |
|--------|-------------|----------|
| London-NY overlap = 13:00–16:00 UTC | ROADMAP wording, fixed window | ✓ |
| Overlap = exact intersection of London (07-16) and NY (13-22) | Mathematically identical to 13-16 — same result, more verbose | |

**User's choice:** Fixed 13:00–16:00 UTC. Auto-selected.
**Notes:** Mathematically equivalent; ROADMAP-literal phrasing preferred for traceability.

---

## Statistical significance

| Option | Description | Selected |
|--------|-------------|----------|
| 30 trades per bucket (Phase 7 MIN_TRADE_COUNT) | Matches existing threshold in `backtest_4yr_evaluate.py:38`; consistent across phases | ✓ |
| 50 trades per bucket | Stricter; would shrink reportable buckets significantly given ~928 GBPNZD-scalp trades over 4yr split into 5 sessions × 24 hours | |
| 100 trades per bucket | Strictest; only top-volume buckets would qualify | |
| 10 trades per bucket | Looser; risks noise classification | |

**User's choice:** 30 trades per bucket (D-03). Auto-selected.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Mark `insufficient_evidence` in CSV; no filter rule | Honest reporting; downstream (Phase 9) sees the gap explicitly | ✓ |
| Skip thin buckets entirely | Cleaner CSVs but loses information about coverage gaps | |
| Treat thin buckets as "neutral" / "allow by default" | Hides the issue; could let bad windows through if they're rare | |

**User's choice:** First-class `insufficient_evidence` status (D-03). Auto-selected.
**Notes:** Recorded in `<specifics>` of CONTEXT.md — heatmaps render thin buckets as distinct color (e.g., gray), never zero-fill or drop.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Skip CI bands for v1 (point estimates) | Consistent with Phase 7 evaluator outputs; faster compute; defers to Phase 9 if needed | ✓ |
| Bootstrap CI per bucket (1000 resamples) | Statistically rigorous but adds compute and complicates CSV format | |
| Analytical CI (parametric Sharpe) | Lighter than bootstrap but assumes normality | |

**User's choice:** Point estimates only (D-05). Auto-selected.
**Notes:** Bootstrap CI flagged as Phase 9+ enhancement in `<deferred>`.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Sharpe ≥ 0.3 = good window; ≤ -0.2 = bad window | Looser than per-strategy 0.5 because bucket is sub-sample; mid-range generates no rule | ✓ |
| Sharpe ≥ 0.5 (same as Phase 7 strategy gate) | Stricter; could leave many buckets unfiltered |  |
| Sharpe ≥ 0.0 = good (any positive) | Too loose; no edge above zero is not an edge | |

**User's choice:** 0.3 / -0.2 split (D-04). Auto-selected.

---

## Filter contract

| Option | Description | Selected |
|--------|-------------|----------|
| Hard veto (`False` blocks entry) | Treated identically to regime gate by Phase 9 router | ✓ |
| Soft warning (router can override) | Adds policy complexity; defeats the point of having a filter | |
| Score-based (returns 0-1, router decides threshold) | More flexible but pushes policy into the router; v1 doesn't need flexibility | |

**User's choice:** Hard veto (D-06). Auto-selected.

---

| Option | Description | Selected |
|--------|-------------|----------|
| `(pair, strategy, timeframe, ts)` exact ROADMAP signature | Maximum specificity; router can short-circuit if any combo is False | ✓ |
| `(pair, ts)` only | Coarser; loses strategy-specific time edges | |
| `(strategy, ts)` only | Loses pair-specific session preferences (USDJPY likes Tokyo, AUDNZD likes Sydney) | |

**User's choice:** Full 4-tuple (D-06). Auto-selected — matches ROADMAP exactly.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Global `is_blackout_window(ts)` ts-only | Simple; pair-specific filtering happens via `is_tradeable_session` | ✓ |
| Per-currency `is_blackout_window(currency, ts)` | More precise but duplicates logic across `is_tradeable_session` | |

**User's choice:** Global ts-only (D-07). Auto-selected.
**Notes:** Currency scoping happens inside `is_tradeable_session` (which knows the pair); blackout windows in `risk_calendar.yaml` declare affected currencies but the predicate API stays simple.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Generated `session_config.py` (Python literals) | Importable by Phase 9 with no parser; matches `pair_config.py` precedent | ✓ |
| External YAML/JSON loaded at runtime | More portable but adds parser dependency and runtime config-not-found edge cases | |
| SQLite table | Overkill for static filter rules; cache.py is for OHLCV not config | |

**User's choice:** Generated `session_config.py` (D-08). Auto-selected.

---

## Risk calendar source

| Option | Description | Selected |
|--------|-------------|----------|
| Empirical detection from 4yr OHLCV (no external feed) | Self-contained; reproducible; no API dependencies; risk_calendar.yaml emitted parametrically | ✓ |
| Manually curated central-bank schedules (FOMC, BoE, etc.) | Most authoritative for known events but misses surprises and requires per-bank schedule maintenance | |
| Live feed integration (forexfactory.com, ICS feeds) | Most current but adds runtime API dependency and breaks the offline-deterministic property | |
| Hybrid: empirical + manual override | Best of both worlds — captured in D-12 as merge with manual override support | |

**User's choice:** Empirical detection (D-09) with manual-override support (D-12). Auto-selected.
**Notes:** Empirical is primary; manual is an additive layer (`source: manual` tag) for known FOMC release minutes etc.

---

| Option | Description | Selected |
|--------|-------------|----------|
| 2.5σ above same-(hour, dow) baseline | ~99th percentile; captures NFP / FOMC / BoE statements without normal-vol windows | ✓ |
| 3.0σ (~99.7th percentile) | Stricter; would miss some BoJ / RBA events that are smaller-magnitude | |
| 2.0σ (~95th percentile) | Looser; captures too many normal-vol news minutes | |

**User's choice:** 2.5σ (D-10). Auto-selected.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — manual entries merge with empirical | Operator can pre-load known FOMC schedule for forward dates not yet in 4yr corpus | ✓ |
| No — empirical only | Forces re-fitting after each cache window extension; misses known forward events | |

**User's choice:** Manual override supported (D-12). Auto-selected.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Per-currency scoping (`affects: ["USD"]`) | NFP affects USD pairs only; ECB affects EUR pairs; BoJ affects JPY pairs | ✓ |
| Global scoping (any blackout blocks all pairs) | Conservative but blocks valid GBPAUD trades during NFP — overkill | |
| Per-pair manual mapping | Most granular but enormous mapping surface | |

**User's choice:** Per-currency (D-13). Auto-selected.

---

## Claude's Discretion

Areas where Claude has flexibility during planning/implementation:
- Specific seaborn colormap choices for heatmaps (diverging vs sequential)
- CSV column ordering details (as long as columns include the named metrics)
- Internal class structure of `temporal_analysis.py` (functional vs class-based)
- Parametric pattern grammar in `risk_calendar.yaml` (token taxonomy for "first_friday_of_month" etc.)
- Whether to emit a Markdown summary report alongside CSVs (recommend yes — single `summary.md` per phase)

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section:
- Bootstrap confidence intervals on per-bucket Sharpe (revisit Phase 9+)
- Regime × session interaction (ROADMAP defers to Phase 9)
- Live event-feed integration (Phase 10+ if router operation reveals missed events)
- Interactive HTML heatmaps (follow-up phase)
- Cross-pair correlation analysis (Phase 9 router-policy concern)
- Tick-level blackout detection (overkill for 30-min window granularity)
