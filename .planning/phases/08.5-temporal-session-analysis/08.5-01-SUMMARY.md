---
phase: 08.5-temporal-session-analysis
plan: 01
subsystem: testing
tags: [pytest, ruamel.yaml, fixtures, red-scaffold, sess-01, sess-02, sess-03, sess-04, temporal-analysis]

# Dependency graph
requires:
  - phase: 08.4-infrastructure-prereqs-ohlcv-cache-rag-learning-loop-trade-replay
    provides: "OHLCVCache + PitClock + conftest fixture-discovery bridge (P02)"
  - phase: 08-hmm-garch-regime-pit-port
    provides: "PitClock + FutureBarReadError + pit_active() (Phase 8 contract)"
provides:
  - "SESS-01..04 first-class requirements in REQUIREMENTS.md (D-18)"
  - "ruamel.yaml>=0.19.0 declared as runtime dep (Plan 04 prereq)"
  - "15 RED tests across 3 files driving Plans 02-05 contracts"
  - "synthetic_trades_factory + synthetic_bars_with_spikes fixtures wired through conftest bridge"
affects: [08.5-02, 08.5-03, 08.5-04, 08.5-05, 09]

# Tech tracking
tech-stack:
  added: ["ruamel.yaml>=0.19.0 (declared, install operator-side)"]
  patterns:
    - "Wave 0 RED scaffold: 15 test fns drive contracts for downstream waves (mirrors 07-01 / 08-01 / 08.4-01)"
    - "Synthetic-data factory fixtures (parametric trades + spike-injected bars) — seed-controlled, no external data"
    - "conftest fixture-discovery bridge extended with two new fixtures preserving Phase 8.4 P02 separation pattern"

key-files:
  created:
    - "V2/tests/v3_intelligence/test_temporal_bucketing.py — 7 RED tests (SESS-01 + SESS-02)"
    - "V2/tests/v3_intelligence/test_risk_calendar.py — 5 RED tests (SESS-03)"
    - "V2/tests/v3_intelligence/test_session_filters.py — 3 RED tests (SESS-04)"
    - ".planning/phases/08.5-temporal-session-analysis/08.5-01-SUMMARY.md (this file)"
  modified:
    - ".planning/REQUIREMENTS.md — SESS-01..04 + traceability (24→28 reqs)"
    - "V2/pyproject.toml — added ruamel.yaml>=0.19.0"
    - "V2/tests/v3_intelligence/conftest_infra.py — +synthetic_trades_factory, +synthetic_bars_with_spikes"
    - "V2/tests/v3_intelligence/conftest.py — re-export both new fixtures via bridge"

key-decisions:
  - "Wave 0 RED scaffold = 15 test fns / 15 collected items across 3 files — mirrors Phase 8.4 P01 32-fn pattern, scaled to 4 SESS reqs"
  - "test_pit_clamp_no_future_leak passes GREEN at scaffold time (PitClock already exists from Phase 8) — serves as regression guard for Phase 8.5 wrapper convention rather than RED contract; not a deviation per plan intent (Tests 1-5 are Plan 02 targets, this one is contract documentation)"
  - "ruamel.yaml install deferred to operator (project requires-python>=3.12; agent env Python 3.10 cannot install). Plan 04 RED tests (test_yaml_roundtrip_preserves_comments + test_manual_override_merge) gate the install — no GREEN before install"
  - "conftest.py existing imports differ from plan's expected signature (sample_trade/in_memory_logger/mock_chroma_collection/mock_psycopg_conn instead of synthetic_three_regime_returns/v1_baseline/mock_supabase_url) — preserved exactly per plan instruction 'keep all existing imports and append only the two new ones'"

patterns-established:
  - "Synthetic spike injection: 1st-Friday 12:30 UTC pattern with controllable magnitude — reusable for any pattern detection test in Phases 8.5+ / 9"
  - "Parametric pattern grammar in cluster_into_patterns return: {pattern: 'wom', n: 1, dow: 4, time: '12:30', duration_min: 30, affects: ['USD'], source: 'empirical'} — Plan 04 reference for risk_calendar.yaml schema"
  - "session_config.py required exports (D-08): SESSION_RULES (dict keyed by (pair, strategy, tf) tuple), BLACKOUT_PATTERNS (list), GENERATED_AT (ISO str), SOURCE_HASH (str) — Plan 05 emitter contract"

requirements-completed: []  # SESS-01..04 are DEFINED in this plan, not COMPLETED — Plans 02-05 close them

# Metrics
duration: 7min
cost: "-"
completed: 2026-04-27
---

# Phase 8.5 Plan 01: Wave 0 RED Scaffold for Temporal & Session Analysis Summary

**15 RED tests across 3 files + 2 synthetic-data fixtures + SESS-01..04 requirements + ruamel.yaml dep — drives the contract for Plans 02-05 implementation of session bucketing, heatmap rendering, risk-calendar detection, and Phase 9 router predicates.**

## Performance

- **Duration:** 7 min
- **API Cost:** -
- **Started:** 2026-04-27T15:31:47Z
- **Completed:** 2026-04-27T15:38:45Z
- **Tasks:** 6/6
- **Files modified:** 7 (4 modified + 3 created + this SUMMARY)

## Accomplishments

- SESS-01..04 added to REQUIREMENTS.md as first-class checkbox requirements with full text per CONTEXT D-18; traceability table extended with 4 Phase 8.5 rows; coverage updated 24→28
- ruamel.yaml>=0.19.0 declared in V2/pyproject.toml dependencies (Plan 04 prereq for risk_calendar.yaml round-trip)
- 15 RED tests collect cleanly: 7 in test_temporal_bucketing.py (SESS-01 + SESS-02), 5 in test_risk_calendar.py (SESS-03), 3 in test_session_filters.py (SESS-04)
- 2 synthetic-data fixtures landed in conftest_infra.py: `synthetic_trades_factory` (parametric trades with controllable hour/pnl distribution) and `synthetic_bars_with_spikes` (4yr OHLC bars with injected first-Friday-12:30 spikes)
- conftest.py bridge extended (Phase 8.4 P02 Rule-3 pattern) so pytest auto-discovers both new fixtures across all v3_intelligence test files
- Phase 6/7/8/8.4 fast-suite regression: 77 passed, 0 failed (baseline preserved; full v3_intelligence collection grew 77→92 with the +15 new RED items)

## Task Commits

Each task was committed atomically:

1. **Task 1: SESS-01..04 in REQUIREMENTS.md** — `f95141c` (feat)
2. **Task 2: ruamel.yaml dep in V2/pyproject.toml** — `57397eb` (chore)
3. **Task 3: test_temporal_bucketing.py — 7 RED tests** — `b8d7f46` (test)
4. **Task 4: test_risk_calendar.py — 5 RED tests** — `df052a5` (test)
5. **Task 5: test_session_filters.py — 3 RED tests** — `4c02cb0` (test)
6. **Task 6: synthetic-data fixtures + conftest bridge** — `c1688fd` (test)

**Plan metadata commit:** pending (final docs commit at end of execution).

## Files Created/Modified

### Created
- `V2/tests/v3_intelligence/test_temporal_bucketing.py` (143 lines) — 7 RED tests for SESS-01 (assign_session, bucket_trades, generate_trades dispatcher, PitClock wrap) + SESS-02 (RENDER_KWARGS RdYlGn diverging cmap, build_heatmap_mask thin-bucket gating)
- `V2/tests/v3_intelligence/test_risk_calendar.py` (133 lines) — 5 RED tests for SESS-03 (detect_blackout_bars 2.5σ z-score, cluster_into_patterns wom grammar, ruamel.yaml comment-preserving round-trip, manual+empirical merge, per-currency affects scoping)
- `V2/tests/v3_intelligence/test_session_filters.py` (85 lines) — 3 RED tests for SESS-04 (is_tradeable_session hard-veto, is_blackout_window global, session_config.py required exports + tuple-keyed SESSION_RULES)

### Modified
- `.planning/REQUIREMENTS.md` — added §"Temporal & Session Analysis (Phase 8.5)" with 4 SESS checkboxes; 4 traceability rows; coverage updated 24→28; footer stamp 2026-04-27 Phase 8.5 opened
- `V2/pyproject.toml` — appended `"ruamel.yaml>=0.19.0",` to `[project] dependencies` (7→8 entries; pure-Python parser for comment-preserving round-trip per CONTEXT D-12)
- `V2/tests/v3_intelligence/conftest_infra.py` — appended Phase 8.5 fixtures section (`synthetic_trades_factory` factory + `synthetic_bars_with_spikes` factory); pre-existing fixtures untouched
- `V2/tests/v3_intelligence/conftest.py` — extended bridge import: added `synthetic_trades_factory` + `synthetic_bars_with_spikes` to existing `from .conftest_infra import (…)` block; pre-existing 4 names preserved exactly

## Decisions Made

- **15-test scaffold (7+5+3) matches CONTEXT.md / VALIDATION.md task IDs** — Plan 02 turns Tests 1-5 of test_temporal_bucketing.py GREEN; Plan 03 turns Tests 6-7 GREEN; Plan 04 turns all 5 of test_risk_calendar.py GREEN; Plan 05 turns all 3 of test_session_filters.py GREEN.
- **`test_pit_clamp_no_future_leak` passes at scaffold time** — Plan intent for this test is to document the wrapper convention (analysis runs inside PitClock(end_ts), pit_active() True/False semantics). Since PitClock already exists from Phase 8, the test's contract is already satisfied; it now serves as a regression guard against any future PitClock breakage. Plan-stated success criterion "All 15 tests fail RED" is therefore relaxed to "All 15 tests collect cleanly with the contract Plans 02-05 must satisfy" — test 5 is contract-as-already-met.
- **Coverage stamp 24→28** matches the expanded SESS-01..04 set; INFRA-01..04 plus the original 20 BRDG/BKTS/REGM/ROUT/LIVE = 24 baseline, plus 4 SESS = 28.
- **Operator-side ruamel.yaml install deferred** — agent env Python 3.10 ≠ project requires-python>=3.12. Verifying `import ruamel.yaml` at scaffold time would falsely fail; Plan 04's RED tests (`test_yaml_roundtrip_preserves_comments`, `test_manual_override_merge`) act as the de facto gate — they cannot turn GREEN until the operator runs `cd V2 && pip install -e .` in a 3.12 env.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `requirements mark-complete` tool flipped SESS-01..04 to Complete prematurely**
- **Found during:** State updates (after final commits)
- **Issue:** The plan's frontmatter declares `requirements: [SESS-01..04]`, so `gsd-tools requirements mark-complete` automatically checked the four boxes `[ ]` -> `[x]` and updated traceability "Pending" -> "Complete". This is wrong — Plan 01 only DEFINES the requirements + creates RED tests (Wave 0 scaffold). The actual implementation (`temporal_analysis.py`, `session_config.py`, `temporal_filters.py`, evidence CSVs/PNGs/risk_calendar.yaml) lands in Plans 02-05. None of those modules exist yet; all 14 RED tests still fail.
- **Fix:** Reverted SESS-01..04 to `[ ]` Pending in both the requirements list AND the traceability table. Plans 02-05 mark them Complete as their own implementations land.
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Verification:** `grep -c '^- \[ \] \*\*SESS-' .planning/REQUIREMENTS.md` returns 4; `grep -c '^| SESS-0[1-4] | Phase 8.5 | Pending |'` returns 4; `grep -c '^- \[x\]'` returns 15 (unchanged from baseline)
- **Lesson:** Wave 0 scaffold plans should declare `requirements: []` in frontmatter (or a separate `requirements_opened: [...]` field) to avoid this auto-flip. Plans that close requirements use `requirements: [...]`. Future Phase 8.5 RED-scaffold-style plans should follow this convention.
- **Committed in:** Pending — folded into the final docs commit (alongside SUMMARY.md, STATE.md, ROADMAP.md)

**2. [Rule 3 - Blocking] conftest.py existing import differed from plan's expected signature**
- **Found during:** Task 6 (fixture bridge update)
- **Issue:** Plan example showed existing import as `from .conftest_infra import synthetic_three_regime_returns, v1_baseline, mock_psycopg_conn, mock_supabase_url`. Actual current import was `sample_trade, in_memory_logger, mock_chroma_collection, mock_psycopg_conn`. The plan-specified `replace_all` would have failed with no-match.
- **Fix:** Followed plan's contingency instruction explicitly: "If the actual existing imports differ (e.g., different fixture names), keep them all and append only the two new ones." Edited the existing 4-line import block to add `synthetic_trades_factory` + `synthetic_bars_with_spikes` only.
- **Files modified:** `V2/tests/v3_intelligence/conftest.py`
- **Verification:** `grep -c "synthetic_trades_factory" conftest.py` returns 1; pre-existing names (`sample_trade`, `in_memory_logger`, `mock_chroma_collection`, `mock_psycopg_conn`) all preserved; full v3_intelligence collection grew exactly 77→92 (+15 new RED).
- **Committed in:** `c1688fd` (Task 6 commit)

---

**Total deviations:** 2 auto-fixed (1 bug-revert, 1 blocking)
**Impact on plan:** Both auto-fixes are housekeeping. The Rule 1 revert prevents downstream Plan 02-05 confusion (their REQUIREMENTS.md flips will be wrong if the boxes are already checked). The Rule 3 import-list extension was explicitly anticipated by the plan ("If the actual existing imports differ … keep them all and append only the two new ones"). No scope creep, no semantic change.

## Issues Encountered

- **`tomllib` not available in Python 3.10 agent env** — used `python3 -c` fallback parser to verify pyproject.toml dependency count (8 entries, all 7 prior preserved + ruamel.yaml). Project's stated `requires-python>=3.12` means production runs use 3.12 with native tomllib; the verification was scaffold-only.
- **`ruamel.yaml` not pip-installable in agent env** — same Python-version reason. Plan acknowledges this in Task 2 ("If pip install fails (no network in agent env), document in the commit message that install is operator-side"). Commit message documents.

## User Setup Required

**Operator must run `cd V2 && pip install -e .` in a Python 3.12 environment before Plan 04 executes.** This installs ruamel.yaml and any other deps refreshed in pyproject.toml. Plan 04's RED tests (`test_yaml_roundtrip_preserves_comments`, `test_manual_override_merge`) cannot turn GREEN without this install.

## Next Phase Readiness

- **Plan 08.5-02 unblocked** — has 5 explicit GREEN targets (Tests 1-5 of test_temporal_bucketing.py): assign_session, bucket_trades, generate_trades dispatcher, _dispatch_* monkeypatch hooks, PitClock wrap convention. Synthetic_trades_factory available via conftest bridge.
- **Plan 08.5-03 unblocked** — has 2 explicit GREEN targets (Tests 6-7 of test_temporal_bucketing.py): RENDER_KWARGS module constant + build_heatmap_mask helper.
- **Plan 08.5-04 unblocked** (after operator-side ruamel.yaml install) — has 5 GREEN targets (test_risk_calendar.py): detect_blackout_bars, cluster_into_patterns (with wom grammar + source_pair currency scoping), write_risk_calendar (round-trip), manual+empirical merge logic.
- **Plan 08.5-05 unblocked** — has 3 GREEN targets (test_session_filters.py): SESSION_RULES dict + BLACKOUT_PATTERNS list + GENERATED_AT/SOURCE_HASH metadata literals; is_tradeable_session + is_blackout_window predicates.
- **No regressions** in Phase 6/7/8/8.4 fast suite (77 passed, 0 failed).

## Self-Check: PASSED

- FOUND: V2/tests/v3_intelligence/test_temporal_bucketing.py (143 lines, 7 tests)
- FOUND: V2/tests/v3_intelligence/test_risk_calendar.py (133 lines, 5 tests)
- FOUND: V2/tests/v3_intelligence/test_session_filters.py (85 lines, 3 tests)
- FOUND: V2/tests/v3_intelligence/conftest_infra.py (modified — +synthetic_trades_factory, +synthetic_bars_with_spikes)
- FOUND: V2/tests/v3_intelligence/conftest.py (modified — bridge extended)
- FOUND: V2/pyproject.toml (modified — ruamel.yaml>=0.19.0 declared)
- FOUND: .planning/REQUIREMENTS.md (modified — SESS-01..04 + traceability + coverage 28)
- FOUND: commit f95141c (Task 1)
- FOUND: commit 57397eb (Task 2)
- FOUND: commit b8d7f46 (Task 3)
- FOUND: commit df052a5 (Task 4)
- FOUND: commit 4c02cb0 (Task 5)
- FOUND: commit c1688fd (Task 6)
- VERIFIED: 15 RED tests collect (`pytest --collect-only` returns "15 tests collected")
- VERIFIED: 14 fail + 1 sentinel-pass on full run (test_pit_clamp_no_future_leak passes by design)
- VERIFIED: 77 pre-existing tests still pass (no regressions)
- VERIFIED: REQUIREMENTS.md grep counts (4 SESS checkboxes, 4 traceability rows, 28 coverage)

---
*Phase: 08.5-temporal-session-analysis*
*Completed: 2026-04-27*
