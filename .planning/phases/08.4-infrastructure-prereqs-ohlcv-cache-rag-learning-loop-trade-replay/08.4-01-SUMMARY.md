---
phase: 08.4-infrastructure-prereqs-ohlcv-cache-rag-learning-loop-trade-replay
plan: 01
subsystem: infra
tags: [supabase, psycopg, ohlcv-cache, rag, learning-loop, adr, mt5-replay, wave0-red, pytest]

# Dependency graph
requires:
  - phase: 08-hmm-garch-regime-pit-port
    provides: PitClock + UNBOUNDED sentinel + pit.py module — referenced by test_cache.py for pit_active() depth probe
  - phase: 07-backtest-entry-fix-h1-momentum-4yr-validation
    provides: pair_config + 4yr CSVs convention (Title-case Open/High/Low/Close) — INFRA-02 mirrors this for GBPNZD
provides:
  - "INFRA-01..04 added to .planning/REQUIREMENTS.md (D-21 satisfied)"
  - "psycopg[binary]>=3.3 + python-dotenv>=1.0 in V2/pyproject.toml"
  - "V2/.env.example documents SUPABASE_DB_URL Session-pooler format (port 5432)"
  - "V2/migrations/0001_create_bars.sql — verbatim composite-PK DDL committed as provenance (application deferred — see Deviations)"
  - "Wave 0 RED test scaffold: 8 files, 32 test functions, 53 collected items (parametrize expansion)"
  - "Plans 02-04 unblocked: every implementation task has a `<verify>` automated command pointing to a real test on disk"
affects:
  - 08.4-02 (cache implementation will turn 11 INFRA-01 RED tests GREEN)
  - 08.4-03 (GBPNZD H1 4yr fetch + 8-pair H4 fetch will turn 30 INFRA-02 + D-08 RED tests GREEN)
  - 08.4-04 (learning_loop + adr will turn 12 INFRA-03 + INFRA-04 RED tests GREEN)
  - Phase 9 (router replay reads from bars table — once Plan 02 lands)

# Tech tracking
tech-stack:
  added:
    - "psycopg[binary]>=3.3 (Postgres async-capable client; pinned to v3 line for prepare_threshold control with Supabase Session pooler)"
    - "python-dotenv>=1.0 (env loading for V2/.env)"
  patterns:
    - "Wave 0 RED scaffold pattern (mirrors Phase 8 P01): every implementation task in subsequent plans has a `<verify>` command pointing to a test file already on disk — Nyquist compliance"
    - "Phase-scoped conftest_infra.py (separate from Phase 8's conftest.py) avoiding fixture-scope collisions across plan boundaries"
    - "Deterministic doc_id ChromaDB upsert pattern (RESEARCH Pitfall 4): symbol|strategy|entry_date — guarantees idempotent learning loop"
    - "Migration provenance file mirrors what apply_migration sends — operator-readable + re-runnable via psql/MCP/Python"

key-files:
  created:
    - V2/migrations/0001_create_bars.sql
    - V2/tests/v3_intelligence/conftest_infra.py
    - V2/tests/v3_intelligence/test_cache.py
    - V2/tests/v3_intelligence/test_cache_integration.py
    - V2/tests/v3_intelligence/test_gbpnzd_parity.py
    - V2/tests/v3_intelligence/test_h4_provisioning.py
    - V2/tests/v3_intelligence/test_learning_loop.py
    - V2/tests/v3_intelligence/test_backfill_rag.py
    - V2/tests/v3_intelligence/test_adr.py
    - V2/.env.example
  modified:
    - .planning/REQUIREMENTS.md (Task 1 — INFRA-01..04 + traceability)
    - V2/pyproject.toml (Task 1 — psycopg + python-dotenv added)

key-decisions:
  - "Task 2 deferred by operator (SUPABASE_DB_URL not provisioned) — Plan 02 slow tests will RED-block (psycopg connection error) rather than RED-import-only until URL provisioned. Tracked as Phase 8.4 known follow-up."
  - "Task 3 migration application deferred — mcp__supabase__* tools not in resumed executor's tool context AND no SUPABASE_DB_URL for direct psql application. Provenance SQL file committed with re-application playbook (Options A/B/C). bars table will exist in Supabase only after follow-up."
  - "Plan executed in two passes: Task 1 (commit 7aa2cf1) under fresh agent → checkpoint → Task 2 deferred → Tasks 3/4/5 (commits 502df94, 7250313, bb03d2b) under resume agent."

patterns-established:
  - "Resume-after-checkpoint pattern: deferred operator action does NOT block downstream RED-scaffold tasks; downstream tasks ship with explicit RED-blocked-vs-RED-import documentation so Plans 02-04 can still proceed."
  - "Migration provenance committed even when application deferred — DDL on disk is canonical source-of-truth, application is a re-runnable side-effect tracked separately."

requirements-completed: []  # INFRA-01..04 are DEFINED here but not yet IMPLEMENTED — they are turned GREEN by Plans 02-04. Plan 01 only ships the requirement definitions and Wave 0 RED scaffolds. Do NOT mark INFRA-01..04 complete until Plans 02-04 finish.

# Metrics
duration: ~9 min (resume pass — Tasks 3/4/5)
cost: tracked by orchestrator
completed: 2026-04-25
---

# Phase 08.4 Plan 01: Wave 0 RED scaffold + Supabase migration provenance + INFRA-01..04 requirements registered Summary

**Wave 0 RED scaffolding (32 test functions / 53 pytest items across 8 files) registered alongside INFRA-01..04 requirements + psycopg/dotenv deps + bars-table migration provenance — Plans 02-04 fully unblocked. Operator-action checkpoint deferred for SUPABASE_DB_URL provisioning, surfaced as a tracked follow-up rather than a blocker.**

## Performance

- **Duration (resume pass — Tasks 3/4/5):** ~9 min (start 2026-04-26T06:30:49Z, end 2026-04-26T06:39:44Z)
- **Started (Task 1, prior agent):** 2026-04-25 ~19:18 (commit 7aa2cf1)
- **Completed:** 2026-04-26T06:39:44Z
- **Tasks:** 4/5 completed (Task 2 = operator-action checkpoint, deferred)
- **Files modified/created:** 11 (3 modified, 8 created)
- **Pytest collection:** 53 items across 7 RED test files, 0 collection errors
- **Phase 8 regression:** 109 passed (Phase 8 baseline + new sanity tests), 0 unrelated regressions

## Accomplishments

- INFRA-01..04 added to .planning/REQUIREMENTS.md with verbatim ROADMAP §Phase 8.4 wording (D-21 satisfied — Plans 02-04 can now reference them in `requirements:` frontmatter)
- psycopg[binary]>=3.3 + python-dotenv>=1.0 added to V2/pyproject.toml [project] dependencies (importable; verified `import psycopg, dotenv` succeeds)
- V2/.env.example documents SUPABASE_DB_URL Session-pooler URI format with explicit port-5432-vs-6543 warning (RESEARCH Pitfall 1)
- V2/migrations/0001_create_bars.sql committed as provenance — composite PK (pair, timeframe, ts) + NUMERIC(12,5) prices + NUMERIC(20,0) volume + idx_bars_pair_tf_ts_desc + table COMMENT; header documents Options A (MCP) / B (psql) / C (Python) re-application paths
- 8 new test files under V2/tests/v3_intelligence/: conftest_infra.py + 7 test_*.py covering INFRA-01 cache contract, INFRA-02 GBPNZD parity, D-08 8-pair H4 provisioning, INFRA-03 learning_loop + backfill_rag, INFRA-04 compute_adr
- 32 RED test functions (53 collected items with parametrize) ready for Plans 02-04 to turn GREEN — Nyquist compliance achieved
- Phase 8 fast-suite regression GREEN: 109 passed (Phase 8 baseline preserved); 20 failures + 7 errors are exclusively the new Wave 0 RED tests (expected behavior — they fail until Plans 02-04 implement cache.py, learning_loop.py, adr.py, GBPNZD/H4 CSVs)

## Task Commits

Each task was committed atomically:

1. **Task 1: REQUIREMENTS.md INFRA-01..04 + psycopg/dotenv deps + V2/.env.example** — `7aa2cf1` (feat)
2. **Task 2: OPERATOR — Add SUPABASE_DB_URL to V2/.env** — *(deferred per operator resume-signal "deferred")*
3. **Task 3: Apply 0001_create_bars Supabase migration via MCP + provenance file** — `502df94` (feat) — *partial: provenance file committed; MCP application deferred (see Deviations)*
4. **Task 4: RED scaffolds — conftest_infra + test_cache (8) + test_cache_integration (3 slow)** — `7250313` (test)
5. **Task 5: RED scaffolds — test_gbpnzd_parity (6) + test_h4_provisioning (3×8 parametrize) + test_learning_loop (6) + test_backfill_rag (2 slow) + test_adr (4)** — `bb03d2b` (test)

**Plan metadata commit:** *(forthcoming — final commit at end of plan)*

## Files Created/Modified

### Created
- `V2/migrations/0001_create_bars.sql` — Verbatim DDL for `bars` table (composite PK + descending idx) with provenance header documenting application status and re-run playbook
- `V2/.env.example` — Operator-readable Supabase URI template with port-5432 warning (Task 1)
- `V2/tests/v3_intelligence/conftest_infra.py` — 4 fixtures: sample_trade, in_memory_logger, mock_chroma_collection, mock_psycopg_conn
- `V2/tests/v3_intelligence/test_cache.py` — 8 RED unit tests for OHLCVCache (D-03 Title-case columns, D-04 PiT/auto-pull/idempotency, Pattern 2 depth probe, RuntimeError on missing DB_URL)
- `V2/tests/v3_intelligence/test_cache_integration.py` — 3 RED slow tests for OHLCVCache (real Supabase round-trip, idempotent upsert, scripts.update_cache CLI)
- `V2/tests/v3_intelligence/test_gbpnzd_parity.py` — 6 RED tests for INFRA-02 (CSV existence, row count >= 15k, Title-case cols, >= 4yr span, Supabase parity, pair_config sanity)
- `V2/tests/v3_intelligence/test_h4_provisioning.py` — 3 parametrized × 8 pairs = 24 collected items for D-08 (CSV existence, row count >= 4k, cache rows)
- `V2/tests/v3_intelligence/test_learning_loop.py` — 6 RED tests for INFRA-03 (SQLite write, RAG index, decision_log on param change, no decision_log unchanged, Chroma idempotency, lazy default logger)
- `V2/tests/v3_intelligence/test_backfill_rag.py` — 2 RED slow tests for D-14 (count parity + idempotent re-run)
- `V2/tests/v3_intelligence/test_adr.py` — 4 RED tests for INFRA-04 (positive float, mean(H-L) equality, lookback window, signature contract)

### Modified
- `.planning/REQUIREMENTS.md` — Inserted §Infrastructure (Phase 8.4) subsection with INFRA-01..04 verbatim ROADMAP wording; added 4 traceability rows; bumped Coverage 20→24 (Task 1)
- `V2/pyproject.toml` — Added 2 deps to [project] dependencies (Task 1)

## Decisions Made

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Task 3 partial completion (provenance file only) | mcp__supabase__* tools not present in resumed executor; SUPABASE_DB_URL not provisioned (Task 2 deferred). Provenance file is the canonical record; application is a re-runnable side-effect with three documented paths. | Tracked as Phase 8.4 known follow-up |
| Continue Tasks 4/5 despite Task 2 + Task 3 partial | Tasks 4/5 don't need a live DB — RED scaffolds are mock-based for unit tests and `_skip_if_no_db_url()` guarded for slow integration tests. Stopping here would block Plans 02-04 verification gates unnecessarily. | Plans 02-04 unblocked |
| RED-block vs RED-import distinction documented | When SUPABASE_DB_URL is provisioned later, slow tests will fail with assertion error (real Supabase round-trip results) rather than ImportError; both are valid RED states for Wave 0 but the operator should know which to expect post-provisioning. | Documented in commit messages + this SUMMARY |

## Deviations from Plan

### Auto-fixed Issues

None — Tasks 4 and 5 followed the plan exactly as written.

### Deferred / Blocked

**1. [Rule 4-equivalent — operator-driven gate] Task 2 deferred by operator: SUPABASE_DB_URL not provisioned**
- **Found during:** Task 2 checkpoint (prior agent)
- **Issue:** Operator chose "deferred" rather than "approved" at the human-action checkpoint
- **Resolution:** Resume agent (this pass) proceeded under deferred-mode rules per orchestrator instructions
- **Downstream impact:** Plan 02's `test_cache_integration.py` slow tests will be RED-blocked (psycopg connection error / `_skip_if_no_db_url()` skip rather than RED-import-only) until URL provisioned
- **Follow-up:** Operator must complete Task 2's how-to-verify steps when ready (Supabase Dashboard → Connect → Session pooler → URI; add `SUPABASE_DB_URL=postgresql://postgres.nubmgoyyndtolsjyynln:...:5432/postgres` to V2/.env)

**2. [Tooling gate] Task 3 partial: bars-table migration application deferred to follow-up**
- **Found during:** Task 3 (resume agent)
- **Issue:** mcp__supabase__apply_migration / mcp__supabase__execute_sql / mcp__supabase__list_tables NOT injected into the resumed executor's tool context (only Read/Write/Edit/Bash available). Without those MCP tools AND with no SUPABASE_DB_URL for direct psql application, the migration cannot be sent to Supabase from this agent.
- **What was done:** Wrote `V2/migrations/0001_create_bars.sql` with verbatim DDL (per the plan's `<bars_table_ddl>` block) plus a header comment documenting (a) status = NOT YET APPLIED, (b) three re-application paths (Option A: MCP-enabled agent re-spawn; Option B: `psql "$SUPABASE_DB_URL" -f V2/migrations/0001_create_bars.sql`; Option C: psycopg connect + execute), (c) post-application: migration name `0001_create_bars` will appear in Supabase's `supabase_migrations.schema_migrations` only when applied via MCP or supabase CLI.
- **Verification:** Task 3's `<automated>` verify command (file existence + grep gates for CREATE TABLE / PRIMARY KEY / idx) returns "migration provenance file OK" — GREEN.
- **Plan-level impact:** Plan 02's cache.py implementation can be developed (unit tests use mocked psycopg) but Plan 02's slow integration tests will RED-block until the bars table actually exists. Plans 03-04 are unaffected by this deferral.
- **Committed in:** 502df94 (Task 3 commit message documents the partial status)
- **Follow-up:** When SUPABASE_DB_URL is provisioned, run Option B (psql) or Option C (Python) from the project root. Optionally, re-spawn an MCP-enabled executor and call `mcp__supabase__apply_migration(name="0001_create_bars", query=<file contents>)` to register the migration in `supabase_migrations.schema_migrations`.

## Operator Checkpoint Outcome (Task 2)

**Resume signal received:** `deferred`

The operator chose to defer SUPABASE_DB_URL provisioning. Per the plan's resume-signal contract: "if unable to provision (e.g., dashboard inaccessible), type 'deferred' and Task 3 will skip the smoke-check; Plan 02's cache tests will be RED-blocked rather than RED-import-only until URL is provisioned." This SUMMARY records that contract and downstream implications for Plans 02-04.

## Authentication Gates

**Active gate:** Supabase project `nubmgoyyndtolsjyynln` access — operator must provision SUPABASE_DB_URL when ready to run the bars-table migration and Plan 02's slow integration tests.

**Status:** Open (deferred). Not blocking Plan 02 cache.py development (unit tests use mocked psycopg) or Plans 03-04. Will become blocking only at Plan 02 verification time IF the operator wants slow integration tests GREEN.

## Phase 8.4 Known Follow-Ups (Tracked)

1. **Operator: Provision SUPABASE_DB_URL** — Supabase Dashboard → Connect → Session pooler → port 5432 → V2/.env. Smoke-test per Task 2 how-to-verify Step 4. (Unblocks Plan 02 slow integration tests + Task 3 application).
2. **Apply 0001_create_bars migration** — three documented paths in V2/migrations/0001_create_bars.sql header. After application, the bars table will be visible via Supabase Dashboard → Table Editor.
3. **Optional: Re-spawn MCP-enabled executor for Task 3** — to register `0001_create_bars` in `supabase_migrations.schema_migrations` (versus applying via psql which doesn't update that table). Cosmetic — does not affect bars-table behavior.

## Known Stubs

None. The Wave 0 RED tests reference modules (`v3_intelligence.cache`, `v3_intelligence.learning_loop`, `v3_intelligence.adr`) that are intentionally absent — they will be created by Plans 02-04. This is the documented Wave 0 RED behavior, not a stub.

## Verification Evidence

```
=== INFRA-0[1-4] count in REQUIREMENTS.md: 9 (4 formal definitions + 4 traceability + 1 coverage line)
=== psycopg/python-dotenv in pyproject.toml: present
    "psycopg[binary]>=3.3",
    "python-dotenv>=1.0",
=== Provenance + env files:
V2/.env.example OK
V2/migrations/0001_create_bars.sql OK
=== 8 test files: all present
=== pytest collect-only: 53 tests collected in 0.32s, 0 collection errors
=== Phase 8 regression (-m "not slow"): 109 passed, 20 failed (Wave 0 RED), 11 skipped, 7 errors (Wave 0 RED)
```

All 20 failures + 7 errors trace to the new V2/tests/v3_intelligence/test_cache.py + test_h4_provisioning.py + test_learning_loop.py — exactly the RED behavior the plan specifies for Wave 0. Phase 8 baseline (109 GREEN) preserved.

## Self-Check: PASSED

All 11 claimed files verified present on disk. All 4 task commits verified in `git log`:
- 7aa2cf1 (Task 1)
- 502df94 (Task 3 partial — provenance only)
- 7250313 (Task 4)
- bb03d2b (Task 5)

