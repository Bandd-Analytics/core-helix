---
plan: 12-01
phase: 12
title: Tier 0 helpers — sm_gmtoffset, sm_WorkTime, sm_WorkTime_no_autogmt × MQ4+MQ5+Python
type: execute
wave: 1
depends_on: []
autonomous: false
files_modified:
  - .gitignore
  - scripts/compile_mq.sh
  - scripts/compile_mq_all_tier.sh
  - V2/v3_intelligence/sm_indicators/__init__.py
  - V2/v3_intelligence/sm_indicators/helpers/__init__.py
  - V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py
  - V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py
  - V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py
  - V2/tests/v3_intelligence/sm_indicators/__init__.py
  - V2/tests/v3_intelligence/sm_indicators/conftest.py
  - V2/tests/v3_intelligence/sm_indicators/helpers/__init__.py
  - V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py
  - V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py
  - V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py
  - resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5
  - resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5
  - resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5
  - resource_pack/MMM/SM Indicators/MT4/_helix_built/README.md
  - resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4
  - resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime.mq4
  - resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime_no_autogmt.mq4
  - resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md
  - resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md
  - resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md
  - resource_pack/MMM/SM Indicators/docs/INDEX.md
requirements: []
must_haves:
  truths:
    - "All 3 helper specs (sm_gmtoffset, sm_WorkTime, sm_WorkTime_no_autogmt) carry an `## Implementation status (Phase 12)` H2 footer with all 3 targets (MQ4/MQ5/Python) marked Built ✅"
    - "All 3 helper Python modules importable as `from v3_intelligence.sm_indicators.helpers import compute_sm_gmtoffset, compute_sm_worktime, compute_sm_worktime_no_autogmt` (or equivalent function names per spec Section 11)"
    - "All 3 helper MQ5 sources compile clean (0 errors, 0 warnings) under Wine MetaEditor"
    - "All 3 helper MQ4 sources compile clean (0 errors, 0 warnings) under Wine MetaEditor"
    - "Each helper has ≥1 GREEN pytest in V2/tests/v3_intelligence/sm_indicators/helpers/"
    - "Phase-12-wide compile script `scripts/compile_mq.sh` exists, executable, and exits 0 on each Tier 0 source"
    - "INDEX.md gains an `## Implementation matrix` section with Tier 0 row populated showing 3/3 ✅"
    - "Tier 0 review checkpoint emitted; user has approved before Plan 12-02 starts"
  artifacts:
    - path: "scripts/compile_mq.sh"
      provides: "Wine MetaEditor compile wrapper with /log: parse + .ex* mtime check"
      min_lines: 30
    - path: "scripts/compile_mq_all_tier.sh"
      provides: "Tier-batch compile wrapper around compile_mq.sh"
      min_lines: 15
    - path: "V2/v3_intelligence/sm_indicators/__init__.py"
      provides: "Package re-exports (initially helpers only)"
    - path: "V2/v3_intelligence/sm_indicators/helpers/__init__.py"
      provides: "Helper sub-package re-exports for compute_sm_gmtoffset, compute_sm_worktime, compute_sm_worktime_no_autogmt"
    - path: "V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py"
      provides: "compute_sm_gmtoffset() function + SMGMTOffsetParams dataclass"
    - path: "V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py"
      provides: "compute_sm_worktime() function + SMWorkTimeParams dataclass"
    - path: "V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py"
      provides: "compute_sm_worktime_no_autogmt() function + SMWorkTimeNoAutoGmtParams dataclass"
    - path: "resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5"
      provides: "MQ5 source — broker GMT offset detector + GlobalVariable publisher"
    - path: "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5"
      provides: "MQ5 source — Asia/London/US session-window box overlay (auto-GMT variant)"
    - path: "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5"
      provides: "MQ5 source — session-window box overlay (manual BrokerGMT variant)"
    - path: "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4"
      provides: "MQ4 source mirror of MQ5 helper"
    - path: "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime.mq4"
      provides: "MQ4 source mirror of MQ5 helper"
    - path: "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime_no_autogmt.mq4"
      provides: "MQ4 source mirror of MQ5 helper"
  key_links:
    - from: "scripts/compile_mq.sh"
      to: "Wine MetaEditor64.exe"
      via: "WINEPREFIX=/home/user/.mt5 wine ... /compile: /log:"
      pattern: "MetaEditor64.exe.*\\/compile:"
    - from: "V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py"
      to: "V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py"
      via: "import compute_sm_gmtoffset (per spec Dependencies section)"
      pattern: "from .sm_gmtoffset import|from \\.sm_gmtoffset"
    - from: "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5"
      to: "GlobalVariable sm_GMTOffset"
      via: "GlobalVariableGet(\"sm_GMTOffset\")"
      pattern: "GlobalVariableGet.*sm_GMTOffset"
    - from: "resource_pack/MMM/SM Indicators/docs/INDEX.md"
      to: "## Implementation matrix"
      via: "new H2 section appended after Phase 11 content"
      pattern: "## Implementation matrix"
---

<objective>
Build runnable code for the 3 Tier 0 helper indicators (sm_gmtoffset, sm_WorkTime, sm_WorkTime_no_autogmt) in MQ4 + MQ5 + Python, set up the Phase-12-wide compile/test scaffold (compile_mq.sh, compile_mq_all_tier.sh, .gitignore for *.ex5/*.ex4, sm_indicators package skeleton, conftest.py with OHLCV fixtures), and gate progression to Plan 12-02 on user review of Tier 0 compile + smoke evidence.

Purpose: Helpers are the most-used components — bugs cascade to Tier 1 + Tier 2. Locking the scaffold + 3 helpers behind a user checkpoint catches structural mistakes before they multiply across 11 more indicators.

Output: 3 helper specs gain `## Implementation status (Phase 12)` footers; 9 source files (3 MQ4 + 3 MQ5 + 3 Python); 3 pytest files; Phase-12-wide compile/test infrastructure; INDEX.md Implementation matrix with Tier 0 row.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sm-indicators-implementation/12-CONTEXT.md
@.planning/phases/12-sm-indicators-implementation/12-RESEARCH.md
@.planning/phases/12-sm-indicators-implementation/12-VALIDATION.md
@resource_pack/MMM/SM Indicators/docs/INDEX.md
@resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md
@resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md
@resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md
@V2/indicators/BandD_WorktimeRibbon.mq5
@V2/indicators/ADR_Levels.mq5
@V2/indicators/RegimeClassifier.mq5
@V2/v3_intelligence/adr.py
@V2/v3_intelligence/regime/__init__.py
@V2/tests/v3_intelligence/conftest.py
@V2/tests/v3_intelligence/test_adr.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from Phase 11 specs + V2 precedents. -->
<!-- Use these directly — no codebase exploration needed. -->

From V2/v3_intelligence/adr.py — canonical Python compute_*() shape (D-11):
```python
from __future__ import annotations
from dataclasses import dataclass
import pandas as pd

@dataclass(frozen=True)
class <Name>Params:
    # spec Section 3 fields with verified-update / [INFER]-tagged defaults

def compute_<snake_name>(df: pd.DataFrame, params: <Name>Params = <Name>Params()) -> pd.DataFrame:
    """Docstring with spec section refs + verified-update notes."""
    out = df.copy()  # NEVER mutate input — Pitfall 3
    # ... computation per spec Section 5 ...
    return out
```

From V2/indicators/BandD_WorktimeRibbon.mq5 — session-ribbon MQ5 idiom (precedent for sm_WorkTime):
```mq5
#property indicator_chart_window
#property indicator_buffers 0
#property indicator_plots   0

input int InpAsiaStartHour   = 0;
input int InpLondonStartHour = 7;
input int InpUSStartHour     = 13;
const string InpObjectPrefix = "smWT_";

int OnInit() { Recompute(); EventSetTimer(60); return INIT_SUCCEEDED; }
void OnDeinit(const int reason) { /* prefix-cleanup loop */ EventKillTimer(); }
int OnCalculate(const int rates_total, const int prev_calculated,
                const datetime &time[], const double &open[], const double &high[],
                const double &low[], const double &close[], const long &tv[],
                const long &v[], const int &sp[]) { return rates_total; }
void OnTimer() { Recompute(); }
```

From V2/v3_intelligence/regime/__init__.py — nested package re-export pattern:
```python
from .emissions import GARCHParams, garch_emission_prob
from .hmm_garch import HMMGARCHRegimeDetector
# (sm_indicators/__init__.py re-exports compute_*() from helpers/ + flat indicator modules)
```

Phase 11 spec footer convention (precedent: Verified Updates appended as new H2 — RESEARCH.md § Spec Footer):
```markdown
---

## Implementation status (Phase 12)

| Target | Status | File | Commit | Date |
|--------|--------|------|--------|------|
| MQ4 | Built ✅ | `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4` | `<sha>` | 2026-04-XX |
| MQ5 | Built ✅ | `resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5` | `<sha>` | 2026-04-XX |
| Python | Built ✅ | `V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py` | `<sha>` | 2026-04-XX |

Tests: `V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py` (`<n>` tests GREEN)
Confidence: Medium (matches Phase 11 spec).
```
</interfaces>
</context>

<tasks>

<!-- ========================================================================= -->
<!-- WAVE 0 — Phase-12-wide scaffold + Tier 0 RED test files                    -->
<!-- ========================================================================= -->

<task type="auto" tdd="true">
  <name>Task 0: Phase-12 scaffold (Wave 0) — compile scripts, .gitignore, sm_indicators package skeleton, conftest.py, 3 RED helper test files</name>
  <files>
    .gitignore,
    scripts/compile_mq.sh,
    scripts/compile_mq_all_tier.sh,
    V2/v3_intelligence/sm_indicators/__init__.py,
    V2/v3_intelligence/sm_indicators/helpers/__init__.py,
    V2/tests/v3_intelligence/sm_indicators/__init__.py,
    V2/tests/v3_intelligence/sm_indicators/conftest.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/__init__.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py,
    resource_pack/MMM/SM Indicators/MT4/_helix_built/README.md
  </files>
  <read_first>
    - .planning/phases/12-sm-indicators-implementation/12-RESEARCH.md (§ MetaEditor Compile Workflow Under Wine; § Pytest Fixture Pattern; § Build Artifact Strategy; § Wave 0 Gaps)
    - .planning/phases/12-sm-indicators-implementation/12-CONTEXT.md (D-05/D-06/D-07 output paths; D-08/D-09/D-10 done criteria)
    - .planning/phases/12-sm-indicators-implementation/12-VALIDATION.md (Plan 12-01 Wave 0 Requirements; Per-Task Verification Map)
    - V2/tests/v3_intelligence/conftest.py (Phase 8 fixture patterns + bridge re-export precedent)
    - V2/tests/v3_intelligence/test_adr.py (explicit-function pytest pattern; mock + synthetic DF helper)
    - .gitignore (current state — Phase 8.4 P04 added V2/reports/* exception, mempalace.yaml override patterns)
    - resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md (Section 10 Test cases)
    - resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md (Section 10 Test cases)
    - resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md (Section 10 Test cases)
  </read_first>
  <behavior>
    - test_sm_gmtoffset.py: ≥3 RED tests asserting compute_sm_gmtoffset / detect_broker_offset returns int hours, falls through manual_gmt when auto_detect=False, returns 0 when broker_ts is None (backtesting mode per RESEARCH Example 2)
    - test_sm_worktime.py: ≥3 RED tests asserting compute_sm_worktime returns DataFrame with `session_label` column ("ASIA"/"LONDON"/"US"/"GAP"/"OFFHOURS"); session boundaries match MMM Book p. 8 (00:30/07:30/13:30 GMT)
    - test_sm_worktime_no_autogmt.py: ≥2 RED tests asserting variant accepts manual broker_gmt: int input + returns same shape; ≥1 test confirming NO sm_gmtoffset import (functional grep)
    - All tests fail with ModuleNotFoundError or AttributeError until Wave 1 lands the implementations
    - conftest.py exposes pytest fixtures: ohlcv_eurusd_h1, ohlcv_usdjpy_h1, ohlcv_gbpnzd_h1, synthetic_ohlc_uptrend, synthetic_doji
    - compile_mq.sh exits 0 ONLY when MetaEditor `/log:` output contains literal `0 errors, 0 warnings` AND `.ex5`/`.ex4` mtime > source mtime
  </behavior>
  <action>
    1. **`.gitignore`** — Append (preserving existing entries; do NOT replace the `*.csv` / `!V2/data/*_4yr.csv` patterns Phase 8.4 established):
       ```
       # Phase 12 — compiled MetaEditor outputs (recompilable from .mq4/.mq5 source)
       *.ex5
       *.ex4
       # But preserve the original non-decompilable Phase 11 source binaries
       !resource_pack/MMM/SM Indicators/MT4/!SM.Indicators/*.ex4
       ```

    2. **`scripts/compile_mq.sh`** — Bash 4+ wrapper around Wine MetaEditor. Required behavior:
       - Usage: `scripts/compile_mq.sh <absolute-or-relative-path-to-.mq5-or-.mq4>`
       - Resolve path → Windows-style via `winepath -w`
       - Invoke: `WINEPREFIX=/home/user/.mt5 wine "/home/user/.mt5/drive_c/Program Files/IC Markets KE MT5 Terminal/MetaEditor64.exe" /compile:"$WINPATH" /log:"$LOGPATH"` (use `timeout 60` to avoid hangs per RESEARCH.md Open Question #1)
       - Parse log: `grep -E "0 errors, 0 warnings" "$LOGPATH"` → required match
       - Mtime check: `[[ "$(stat -c%Y "$EXPATH")" -gt "$(stat -c%Y "$SRCPATH")" ]]` → required pass
       - Exit 0 only if both checks pass; exit 1 otherwise; print log tail on failure
       - Mark executable: `chmod +x scripts/compile_mq.sh`

    3. **`scripts/compile_mq_all_tier.sh`** — Loops `compile_mq.sh` over a tier directory.
       - Usage: `scripts/compile_mq_all_tier.sh <tier-name>` where tier-name is `tier0` | `tier1` | `tier2`
       - tier0 → both `resource_pack/MMM/SM Indicators/MT5/helpers/*.mq5` AND `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/*.mq4`
       - tier1 → indicators dir, files matching SM_ADR_Marker, SM_Daily_HiLo, SM_BPCT, SM_IlsleyPsychLevels, SM_Crossover_Arrows
       - tier2 → indicators dir, the 6 composite specs
       - Aggregate exit code: 0 only if every file passes
       - chmod +x

    4. **`V2/v3_intelligence/sm_indicators/__init__.py`** — Empty package init. Add docstring:
       ```python
       """SM Indicators reconstructions (Phase 12).

       Reconstructs runnable code for the 14 !SM_*/!sm_* MT4 indicators from the
       Phase 11 specs at resource_pack/MMM/SM Indicators/docs/. Function-first
       surface per CONTEXT.md D-11; helpers/ subpackage mirrors docs/helpers/
       layout.
       """
       from __future__ import annotations
       # Tier 0 re-exports added by Plan 12-01 Wave 1; Tier 1 + Tier 2 added later.
       ```

    5. **`V2/v3_intelligence/sm_indicators/helpers/__init__.py`** — Empty for Wave 0 (RED imports must fail). Add docstring only:
       ```python
       """Tier 0 helpers — broker GMT offset + session-window classifiers."""
       from __future__ import annotations
       # Wave 1 will fill: compute_sm_gmtoffset, compute_sm_worktime,
       #                   compute_sm_worktime_no_autogmt
       ```

    6. **`V2/tests/v3_intelligence/sm_indicators/__init__.py`** + **`helpers/__init__.py`** — Empty test packages.

    7. **`V2/tests/v3_intelligence/sm_indicators/conftest.py`** — Per RESEARCH.md § Pytest Fixture Pattern. Required fixtures:
       - `ohlcv_eurusd_h1` — session-scoped, reads `V2/data/EURUSD_H1_4yr.csv` via `pd.read_csv(parse_dates=["timestamp"])` then `.set_index("timestamp")` (Title-case OHLC per Phase 8.4 D-20)
       - `ohlcv_usdjpy_h1` — same shape, USDJPY H1 4yr
       - `ohlcv_gbpnzd_h1` — same, GBPNZD H1 4yr (per INFRA-02 the file exists)
       - `synthetic_ohlc_uptrend` — 100-bar uptrend per RESEARCH Example fixture (lines 502-513)
       - `synthetic_doji` — 100-bar series with Open=Close=Mid alternating, narrow High-Low, used by SM_BPCT shape test
       Use `Path(__file__).resolve().parents[3] / "V2" / "data"` as DATA_DIR.

    8. **`V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py`** — 3 RED tests per spec Section 10:
       - `test_returns_zero_when_broker_ts_none()` — backtesting-mode contract; calls `compute_sm_gmtoffset(SMGMTOffsetParams(), broker_ts=None)` → asserts == 0
       - `test_returns_manual_when_auto_detect_false()` — calls with `SMGMTOffsetParams(auto_detect=False, manual_gmt=3)` → asserts == 3
       - `test_returns_int_hours_from_broker_delta()` — calls with broker_ts = current UTC + 3 hours → asserts result == 3
       - All imports `from v3_intelligence.sm_indicators.helpers.sm_gmtoffset import compute_sm_gmtoffset, SMGMTOffsetParams` (RED until Wave 1).

    9. **`V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py`** — 3 RED tests per spec Section 10:
       - `test_returns_session_label_column()` — calls `compute_sm_worktime(synthetic_ohlc_uptrend)` → asserts `"session_label"` in columns
       - `test_classifies_london_open()` — bar at 07:30 GMT → label == "LONDON"
       - `test_classifies_asia_session()` — bar at 02:00 GMT → label == "ASIA"

    10. **`V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py`** — 3 RED tests:
        - `test_accepts_broker_gmt_input()` — calls `compute_sm_worktime_no_autogmt(df, SMWorkTimeNoAutoGmtParams(broker_gmt=3))` → returns DataFrame with session_label
        - `test_no_sm_gmtoffset_dependency()` — `import inspect; src = inspect.getsource(...); assert "sm_gmtoffset" not in src` (functional grep gate per spec D-19 architectural distinction)
        - `test_same_shape_as_auto_variant()` — output columns identical to `compute_sm_worktime` output

    11. **`resource_pack/MMM/SM Indicators/MT4/_helix_built/README.md`** — Brief one-pager:
        ```markdown
        # _helix_built — Phase 12 SM Indicators reconstructions

        These `.mq4` sources are reconstructions of the original `!SM_*.ex4` /
        `!sm_*.ex4` binaries (in `../!SM.Indicators/`) from the Phase 11 specs
        at `../../docs/`. The originals are non-decompilable; these files are
        rebuilt from indicator names + MMM community knowledge + spec Section 11
        Port notes.

        See `.planning/phases/12-sm-indicators-implementation/12-CONTEXT.md` D-05.
        Compiled `.ex4` outputs are gitignored per Phase 12 Plan 01 (recompilable).
        ```

    12. Verify the 3 RED test files import-fail cleanly (pytest collects them but every test fails with `ModuleNotFoundError` or `AttributeError`). This is the RED state Wave 1 turns GREEN.
  </action>
  <verify>
    <automated>cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/ 2>&1 | grep -E "^(FAILED|ERROR|PASSED|collected|=+ \d+ failed)" | tail -10; ls scripts/compile_mq.sh scripts/compile_mq_all_tier.sh && bash scripts/compile_mq.sh --help 2>&1 | head -3 || true; grep -c "^\*\.ex5$" .gitignore; grep -c "^\*\.ex4$" .gitignore; grep -c "^!resource_pack/MMM/SM Indicators/MT4/!SM.Indicators/\*\.ex4" .gitignore; ls V2/v3_intelligence/sm_indicators/__init__.py V2/v3_intelligence/sm_indicators/helpers/__init__.py V2/tests/v3_intelligence/sm_indicators/conftest.py "resource_pack/MMM/SM Indicators/MT4/_helix_built/README.md"</automated>
  </verify>
  <acceptance_criteria>
    - `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/ -x` → exits non-zero (RED — expected) AND collects ≥9 tests AND every failure is `ModuleNotFoundError` or `AttributeError` (NOT pytest collection / syntax errors)
    - `bash -n scripts/compile_mq.sh` → exits 0 (syntax valid)
    - `[[ -x scripts/compile_mq.sh ]]` → true
    - `[[ -x scripts/compile_mq_all_tier.sh ]]` → true
    - `grep -E "^\*\.ex5$" .gitignore` → matches
    - `grep -E "^\*\.ex4$" .gitignore` → matches
    - `grep -E "^!resource_pack/MMM/SM Indicators/MT4/!SM\.Indicators/\*\.ex4$" .gitignore` → matches
    - File exists: `V2/v3_intelligence/sm_indicators/__init__.py`
    - File exists: `V2/v3_intelligence/sm_indicators/helpers/__init__.py`
    - File exists: `V2/tests/v3_intelligence/sm_indicators/conftest.py`
    - File exists: `V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py`
    - File exists: `V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py`
    - File exists: `V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py`
    - File exists: `resource_pack/MMM/SM Indicators/MT4/_helix_built/README.md`
    - `grep -c "ohlcv_eurusd_h1\|ohlcv_usdjpy_h1\|ohlcv_gbpnzd_h1\|synthetic_ohlc_uptrend\|synthetic_doji" V2/tests/v3_intelligence/sm_indicators/conftest.py` → ≥5
    - Phase 8 + Phase 8.4 + Phase 8.5 fast-suite regression: `cd V2 && pytest -v tests/v3_intelligence/test_adr.py tests/v3_intelligence/test_pit.py tests/v3_intelligence/test_temporal_bucketing.py 2>&1 | tail -3` → no NEW failures (counts unchanged from STATE.md)
  </acceptance_criteria>
  <done>
    Wave 0 RED scaffold lands. 3 helper test files exist and fail RED on missing module imports. Compile scripts ready and executable. .gitignore extended without breaking existing patterns. Phase-12-wide package skeleton in place. README in _helix_built/ documents the directory's purpose.
  </done>
</task>

<!-- ========================================================================= -->
<!-- WAVE 1 — 3 helper indicators × 3 targets, parallel implementation tasks    -->
<!-- ========================================================================= -->

<task type="auto" tdd="true">
  <name>Task 1: sm_gmtoffset × MQ4 + MQ5 + Python (atomic — all 3 targets in one commit)</name>
  <files>
    resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5,
    resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4,
    V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py,
    V2/v3_intelligence/sm_indicators/helpers/__init__.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py,
    resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md
  </files>
  <read_first>
    - resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md (THE CONTRACT — Section 3 Inputs, Section 4 Outputs, Section 5 Calculation logic, Section 10 Test cases, Section 11 Port notes — every action below quotes from this file)
    - .planning/phases/12-sm-indicators-implementation/12-RESEARCH.md (§ Architecture Patterns — Pattern 1 MQ5 header + lifecycle; § Code Examples — Example 1 sm_gmtoffset MQ5 skeleton; § Code Examples — Example 2 sm_gmtoffset Python skeleton; § Common Pitfalls — Pitfall 2, 3)
    - V2/indicators/ADR_Levels.mq5 (MQ5 header + OnInit + EventSetTimer + OnDeinit precedent)
    - V2/v3_intelligence/adr.py (Python compute_*() function shape — D-11 canonical)
    - V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py (the RED tests Wave 0 wrote — Wave 1 turns them GREEN)
  </read_first>
  <behavior>
    - Python: `compute_sm_gmtoffset(params, broker_ts)` returns int hours; backtesting (broker_ts=None) returns 0; manual mode (auto_detect=False) returns params.manual_gmt; auto mode computes round((broker_ts - utc_now).total_seconds() / 3600)
    - MQ5: `OnInit` calls `ComputeOffset()` and publishes `GlobalVariableSet("sm_GMTOffset", offset_hours)`; `EventSetTimer(3600)` for hourly refresh; `OnDeinit` calls `EventKillTimer()` and clears `Comment("")` but does NOT delete the GlobalVariable (per spec Section 5 step 4)
    - MQ4: same logical structure with MQL4 idioms (`int init()`/`int deinit()` legacy OR `OnInit`/`OnDeinit` 5xx-build form — pick whichever ADR_Levels precedent uses); same GlobalVariable contract
    - Compile: 0 errors / 0 warnings on both MQ4 and MQ5 sources
    - Tests: 3+ pytest functions GREEN
  </behavior>
  <action>
    1. **Build MQ5 source at `resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5`** — Use RESEARCH.md § Code Examples — Example 1 verbatim as starting skeleton. Required elements:
       - Header block citing spec path `resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md`, Phase 12 Plan 01, D-06/D-19
       - `#property indicator_chart_window`, `#property indicator_buffers 0`, `#property indicator_plots 0`
       - Inputs (per spec Section 3): `input bool InpAutoDetect = true`, `input int InpManualGMT = 0`, `input bool InpDSTAdjust = true`, `input string InpGlobalVarName = "sm_GMTOffset"`
       - `int OnInit()` — calls `ComputeOffset()`, `GlobalVariableSet(InpGlobalVarName, (double)offset_hours)`, `Comment(...)`, `EventSetTimer(3600)`
       - `void OnTimer()` — re-runs ComputeOffset + GlobalVariableSet
       - `void OnDeinit(const int reason)` — `EventKillTimer()`, `Comment("")` — DO NOT delete GlobalVariable (spec Section 5 step 4)
       - `int OnCalculate(const int rates_total, const int prev_calculated, const datetime &time[], const double &open[], const double &high[], const double &low[], const double &close[], const long &tv[], const long &v[], const int &sp[])` — full MQ5 signature per D-19; returns `rates_total` (no per-bar work)
       - `int ComputeOffset()` — `(int)MathRound(((long)TimeCurrent() - (long)TimeGMT()) / 3600.0)` with optional DST subtraction (-1 hours when month in [3..10] per spec Section 5 step 3)
       - Compile: `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5"` → exit 0

    2. **Build MQ4 source at `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4`** — Same logical structure, MQL4 idioms per D-20 + spec Section 11 "MQ4 → MQ5 deltas" subsection (in reverse):
       - `#property indicator_chart_window`, NO `#property indicator_buffers` (MQ4 default)
       - Same `extern` (or `input`, MQL4 5xx accepts both) declarations
       - `int init()` (or `int OnInit()`) — same logical body
       - `int start()` (or `OnCalculate`) — return 0 (no per-bar work)
       - `int deinit()` — same cleanup
       - Compile: `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4"` → exit 0

    3. **Build Python module at `V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py`** — Use RESEARCH.md § Code Examples — Example 2 verbatim:
       ```python
       from __future__ import annotations
       from dataclasses import dataclass
       from datetime import datetime, timezone
       from typing import Optional

       @dataclass(frozen=True)
       class SMGMTOffsetParams:
           auto_detect: bool = True
           manual_gmt: int = 0
           broker_iana_tz: str = "Europe/Nicosia"  # IC Markets EU default

       def compute_sm_gmtoffset(params: SMGMTOffsetParams = SMGMTOffsetParams(),
                                broker_ts: Optional[datetime] = None) -> int:
           """Per spec sm_gmtoffset.md Section 11 Python port.

           Returns broker's effective GMT offset in integer hours.
           Backtesting (broker_ts=None) → 0 (UTC). Manual (auto_detect=False) →
           params.manual_gmt. Auto mode → round((broker_ts - utc_now)/3600).
           """
           if not params.auto_detect:
               return params.manual_gmt
           if broker_ts is None:
               return 0
           utc_now = datetime.now(timezone.utc)
           delta_seconds = (broker_ts - utc_now.replace(tzinfo=None)).total_seconds()
           return round(delta_seconds / 3600)

       __all__ = ["SMGMTOffsetParams", "compute_sm_gmtoffset"]
       ```

    4. **Update `V2/v3_intelligence/sm_indicators/helpers/__init__.py`** — Add re-export:
       ```python
       from .sm_gmtoffset import SMGMTOffsetParams, compute_sm_gmtoffset
       ```

    5. **Convert spec Section 10 test cases to pytest** — Edit `V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py` (Wave 0 RED → GREEN). Tests must remain the 3 written in Wave 0; just verify they pass with the new implementation. If spec Section 10 lists ADDITIONAL cases (e.g., DST month edge, JPY broker), add them as new test functions.

    6. **Append `## Implementation status (Phase 12)` H2 section** to `resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md`. Append AFTER any existing Section 12 Uncertainty log + Verified Updates footer (per RESEARCH.md § Spec Footer placement decision). Exact markdown:
       ```markdown

       ---

       ## Implementation status (Phase 12)

       | Target | Status | File | Commit | Date |
       |--------|--------|------|--------|------|
       | MQ4 | Built ✅ | `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4` | `<TBD>` | 2026-04-XX |
       | MQ5 | Built ✅ | `resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5` | `<TBD>` | 2026-04-XX |
       | Python | Built ✅ | `V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py` | `<TBD>` | 2026-04-XX |

       Tests: `V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py` (3+ tests GREEN)
       Confidence: Medium (matches Phase 11 spec).
       ```
       Replace `<TBD>` placeholder with actual commit SHA after the implementation commit lands (executor or post-commit hook). Date placeholder uses today's actual date.

    7. Run `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py` → all GREEN.
  </action>
  <verify>
    <automated>cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py && cd .. && bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5" && bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4" && grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md"</automated>
  </verify>
  <acceptance_criteria>
    - `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_gmtoffset.py` → exits 0; ≥3 PASSED
    - `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5"` → exits 0
    - `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4"` → exits 0
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py` exists and contains `def compute_sm_gmtoffset(`
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py` contains `class SMGMTOffsetParams`
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5` contains `#property indicator_chart_window`
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5` contains `GlobalVariableSet`
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_gmtoffset.mq5` contains `EventSetTimer(3600)`
    - File `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_gmtoffset.mq4` contains `GlobalVariableSet`
    - `grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md"` → matches
    - `grep -q "from .sm_gmtoffset import" V2/v3_intelligence/sm_indicators/helpers/__init__.py` → matches
    - `python3 -c "from v3_intelligence.sm_indicators.helpers import compute_sm_gmtoffset, SMGMTOffsetParams; print('OK')"` (run from `V2/` cwd) → prints OK
  </acceptance_criteria>
  <done>
    sm_gmtoffset implemented across MQ4 + MQ5 + Python; spec footer appended; tests GREEN; compile scripts pass; functions importable from package.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sm_WorkTime × MQ4 + MQ5 + Python (atomic — all 3 targets in one commit)</name>
  <files>
    resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5,
    resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime.mq4,
    V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py,
    V2/v3_intelligence/sm_indicators/helpers/__init__.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py,
    resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md
  </files>
  <read_first>
    - resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md (THE CONTRACT — quote Section 3, 4, 5, 10, 11 verbatim into actions; specifically MMM Book p. 8 session times 00:30/07:30/13:30 GMT and p. 40 Colour-Coded Sessions; Dependencies: sm_gmtoffset GlobalVariable)
    - .planning/phases/12-sm-indicators-implementation/12-RESEARCH.md (§ Architecture Patterns Pattern 1, Pattern 3 ObjectCreate prefix cleanup; § Common Pitfalls Pitfall 9 font rendering)
    - V2/indicators/BandD_WorktimeRibbon.mq5 (THE primary precedent — session-ribbon with timer + chart-id 0 + multi-day rectangle pattern + font "Arial" workaround)
    - V2/v3_intelligence/sm_indicators/helpers/sm_gmtoffset.py (just-built; sm_WorkTime Python port reads broker offset via this module)
    - V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py (Wave 0 RED tests — Wave 1 turns GREEN)
  </read_first>
  <behavior>
    - Python: `compute_sm_worktime(df, params)` returns DataFrame with new `session_label` column; values in {"ASIA", "LONDON", "US", "GAP", "OFFHOURS"} per spec Section 5 / MMM Book p. 8
    - MQ5: `OnInit` reads `GlobalVariableGet("sm_GMTOffset")` (per spec Dependencies + Section 5); creates OBJ_RECTANGLE per session per day (precedent: BandD_WorktimeRibbon.mq5 Pattern 2); `EventSetTimer(60)` for periodic refresh; `OnDeinit` deletes all objects with prefix "smWT_" (or spec-specified) and `EventKillTimer()`
    - MQ4: same logical structure, MQL4 idioms per D-20
    - Compile clean both MQ4 + MQ5
    - Tests GREEN
  </behavior>
  <action>
    1. **Build MQ5 source at `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5`** — Use BandD_WorktimeRibbon.mq5 as the structural precedent. Required elements:
       - Header citing spec path + Phase 12 Plan 01 + D-06/D-19
       - `#property indicator_chart_window`, `#property indicator_buffers 0`, `#property indicator_plots 0`
       - Inputs per spec Section 3: `input int InpAsiaStartHour = 0`, `input int InpAsiaStartMin = 30`, `input int InpAsiaEndHour = 7`, `input int InpAsiaEndMin = 30`, `input int InpLondonStartHour = 7`, `input int InpLondonStartMin = 30`, `input int InpLondonEndHour = 13`, `input int InpLondonEndMin = 30`, `input int InpUSStartHour = 13`, `input int InpUSStartMin = 30`, `input int InpUSEndHour = 22`, `input int InpUSEndMin = 0`, `input color InpAsiaColor = clrLightYellow`, `input color InpLondonColor = clrLightBlue`, `input color InpUSColor = clrLightGreen`, `input int InpDaysBack = 5`. Default minute fields = 30 per MMM Book p. 8 (00:30/07:30/13:30 GMT). All `[INFER]` for hours-only-vs-minutes — comment `// [INFER] minutes split per spec Section 12 ambiguity` next to the `*Min` inputs.
       - `const string InpObjectPrefix = "smWT_";` (per RESEARCH MQ5 Pattern 3)
       - `int OnInit()` — read `double broker_gmt = GlobalVariableGet("sm_GMTOffset");` (with `if(GlobalVariableCheck(...))` guard per spec Edge cases); call `Recompute()`; `EventSetTimer(60)`
       - `void Recompute()` — for each day in InpDaysBack..0, for each session (Asia/London/US), compute the day's session start/end datetimes (apply `broker_gmt` offset), call `ObjectCreate(0, name, OBJ_RECTANGLE, 0, t1, p1, t2, p2)` with `OBJPROP_TIME` updates per Pattern 3, `OBJPROP_COLOR` per session, `OBJPROP_BACK true` (send-to-back per RESEARCH Pattern 3), `OBJPROP_FONT "Arial"` if any text (Pitfall 9)
       - `OnDeinit` — prefix-cleanup loop per RESEARCH Pattern 3 + `EventKillTimer()`
       - `OnCalculate` — full MQ5 signature; calls `Recompute()` only when current D1 bar changes (cache `last_d1_bar` static)
       - `OnTimer()` — calls Recompute()
       - Compile: 0/0

    2. **Build MQ4 source at `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime.mq4`** — Same logical structure with MQL4 idioms:
       - `#property indicator_chart_window`
       - `extern int AsiaStartHour = 0;` etc. (or `input` per MQL4 5xx)
       - `ObjectCreate(name, OBJ_RECTANGLE, 0, t1, p1, t2, p2)` (no chart-id — D-20)
       - Compile: 0/0

    3. **Build Python module at `V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py`** — Per D-11 shape:
       ```python
       from __future__ import annotations
       from dataclasses import dataclass
       from typing import Optional
       import pandas as pd
       from .sm_gmtoffset import SMGMTOffsetParams, compute_sm_gmtoffset

       @dataclass(frozen=True)
       class SMWorkTimeParams:
           # Per spec Section 3 + MMM Book p. 8 GMT session times
           asia_start_h: int = 0
           asia_start_m: int = 30
           asia_end_h: int = 7
           asia_end_m: int = 30
           london_start_h: int = 7
           london_start_m: int = 30
           london_end_h: int = 13
           london_end_m: int = 30
           us_start_h: int = 13
           us_start_m: int = 30
           us_end_h: int = 22
           us_end_m: int = 0
           gmt_params: SMGMTOffsetParams = SMGMTOffsetParams()

       def compute_sm_worktime(df: pd.DataFrame,
                               params: SMWorkTimeParams = SMWorkTimeParams()) -> pd.DataFrame:
           """Per spec sm_WorkTime.md Section 5 + MMM Book p. 8 / p. 40.

           Args:
               df: OHLCV DataFrame with datetime index (UTC if available;
                   otherwise broker time + gmt_params).
               params: SMWorkTimeParams with verified MMM session boundaries.

           Returns:
               Copy of df with new 'session_label' column in
               {"ASIA", "LONDON", "US", "GAP", "OFFHOURS"}.
           """
           out = df.copy()  # NEVER mutate (Pitfall 3)
           # Detect broker offset (0 in backtest mode per sm_gmtoffset contract):
           offset_h = compute_sm_gmtoffset(params.gmt_params, broker_ts=None)
           # Assume df.index is UTC if naive (Phase 8 PitClock convention)
           ts = out.index
           hour_min = ts.hour * 60 + ts.minute
           asia_start = params.asia_start_h * 60 + params.asia_start_m
           asia_end = params.asia_end_h * 60 + params.asia_end_m
           london_start = params.london_start_h * 60 + params.london_start_m
           london_end = params.london_end_h * 60 + params.london_end_m
           us_start = params.us_start_h * 60 + params.us_start_m
           us_end = params.us_end_h * 60 + params.us_end_m

           import numpy as np
           label = np.full(len(out), "OFFHOURS", dtype=object)
           label[(hour_min >= asia_start) & (hour_min < asia_end)] = "ASIA"
           label[(hour_min >= london_start) & (hour_min < london_end)] = "LONDON"
           label[(hour_min >= us_start) & (hour_min < us_end)] = "US"
           # GAP: between Asia close and London open (07:30 GMT exact = boundary)
           # — already covered by < / >= boundaries above; OFFHOURS is the residual
           out["session_label"] = label
           return out

       __all__ = ["SMWorkTimeParams", "compute_sm_worktime"]
       ```

    4. **Update `V2/v3_intelligence/sm_indicators/helpers/__init__.py`** — Add:
       ```python
       from .sm_worktime import SMWorkTimeParams, compute_sm_worktime
       ```

    5. **Convert spec Section 10 test cases to pytest** — Edit `test_sm_worktime.py` to turn Wave 0 RED tests GREEN. If Section 10 has more cases (e.g., London-NY overlap, GAP between Asia close and London open), add as new test functions per `test_adr.py` explicit-function pattern.

    6. **Append `## Implementation status (Phase 12)` to spec at `resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md`** — same H2 markdown table format as Task 1, paths updated to sm_WorkTime files. Confidence: Medium.

    7. Run pytest + compile scripts → all GREEN / exit 0.
  </action>
  <verify>
    <automated>cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py && cd .. && bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5" && bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime.mq4" && grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md"</automated>
  </verify>
  <acceptance_criteria>
    - `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime.py` → ≥3 PASSED, 0 failed
    - `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5"` → exit 0
    - `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime.mq4"` → exit 0
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py` contains `def compute_sm_worktime(`
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py` contains `class SMWorkTimeParams`
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py` contains `from .sm_gmtoffset import` (dependency declared per spec Section 8)
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5` contains `#property indicator_chart_window`
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5` contains `GlobalVariableGet`
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5` contains `OBJ_RECTANGLE`
    - `grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md"` → matches
    - `python3 -c "from v3_intelligence.sm_indicators.helpers import compute_sm_worktime; print('OK')"` (cwd V2) → prints OK
  </acceptance_criteria>
  <done>
    sm_WorkTime implemented across MQ4 + MQ5 + Python; reads sm_GMTOffset GlobalVariable; spec footer appended; tests GREEN; compile clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sm_WorkTime_no_autogmt × MQ4 + MQ5 + Python (atomic — manual BrokerGMT variant; explicitly NO sm_gmtoffset dep)</name>
  <files>
    resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5,
    resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime_no_autogmt.mq4,
    V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py,
    V2/v3_intelligence/sm_indicators/helpers/__init__.py,
    V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py,
    resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md
  </files>
  <read_first>
    - resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md (THE CONTRACT — Section 8 Dependencies explicitly: NO sm_gmtoffset; manual BrokerGMT input is the architectural distinction; Sep 2011 binary predates Dec 2011 sm_WorkTime)
    - resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5 (just-built; this variant is structurally similar minus the GlobalVariableGet)
    - V2/v3_intelligence/sm_indicators/helpers/sm_worktime.py (just-built; this variant strips the gmt_params field and uses raw broker_gmt int)
    - V2/tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py (Wave 0 RED tests including the no-sm_gmtoffset-import grep gate — Wave 1 turns GREEN)
  </read_first>
  <behavior>
    - Python: `compute_sm_worktime_no_autogmt(df, params)` returns DataFrame with `session_label`; SMWorkTimeNoAutoGmtParams has `broker_gmt: int` field instead of nested `gmt_params`
    - Module MUST NOT `import sm_gmtoffset` (functional grep gate per spec Section 8 architectural distinction)
    - MQ5: same OBJ_RECTANGLE pattern as sm_WorkTime.mq5 BUT reads `input int InpBrokerGMT = 0;` directly instead of `GlobalVariableGet("sm_GMTOffset")`
    - MQ4: same minus chart-id and using extern
    - Compile clean both targets
  </behavior>
  <action>
    1. **Build MQ5 source at `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5`** — Same scaffold as sm_WorkTime.mq5 BUT:
       - Replace `GlobalVariableGet("sm_GMTOffset")` with `input int InpBrokerGMT = 0;` direct read
       - Add `input bool InpBrokerDSTAdjust = true;` per spec Section 3
       - DO NOT call any sm_gmtoffset functions; comment explicitly: `// D-19 architectural distinction — NO sm_gmtoffset dependency, manual BrokerGMT input only`
       - Same OBJ_RECTANGLE per-session per-day rendering, same `smWT2_` prefix (or spec-defined; if spec is silent, use `smWTnoauto_`)
       - Compile: 0/0

    2. **Build MQ4 source at `resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime_no_autogmt.mq4`** — Same logical structure with MQL4 idioms; same no-sm_gmtoffset constraint.

    3. **Build Python module at `V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py`** — Per D-11:
       ```python
       from __future__ import annotations
       from dataclasses import dataclass
       import pandas as pd
       # NOTE: D-19 architectural distinction — this module does NOT import
       # sm_gmtoffset. The broker_gmt field is a manual integer input.

       @dataclass(frozen=True)
       class SMWorkTimeNoAutoGmtParams:
           broker_gmt: int = 0
           broker_dst_adjust: bool = True
           # Same session boundaries as SMWorkTimeParams (MMM Book p. 8)
           asia_start_h: int = 0
           asia_start_m: int = 30
           asia_end_h: int = 7
           asia_end_m: int = 30
           london_start_h: int = 7
           london_start_m: int = 30
           london_end_h: int = 13
           london_end_m: int = 30
           us_start_h: int = 13
           us_start_m: int = 30
           us_end_h: int = 22
           us_end_m: int = 0

       def compute_sm_worktime_no_autogmt(df: pd.DataFrame,
                                           params: SMWorkTimeNoAutoGmtParams = SMWorkTimeNoAutoGmtParams()) -> pd.DataFrame:
           """Per spec sm_WorkTime_no_autogmt.md Section 5.

           Manual-BrokerGMT variant of sm_WorkTime — explicitly NO sm_gmtoffset
           dependency per spec Section 8 architectural distinction.
           """
           out = df.copy()  # NEVER mutate
           # ... same minute-bucketing logic as compute_sm_worktime,
           #     applying params.broker_gmt as offset to df.index ...
           # (Implementation mirrors sm_worktime.py minus the gmt_params lookup)
           return out

       __all__ = ["SMWorkTimeNoAutoGmtParams", "compute_sm_worktime_no_autogmt"]
       ```

    4. **Update helpers/__init__.py** — Add:
       ```python
       from .sm_worktime_no_autogmt import (
           SMWorkTimeNoAutoGmtParams, compute_sm_worktime_no_autogmt,
       )
       ```

    5. **Convert spec Section 10 test cases to pytest** — Turn Wave 0 RED → GREEN. The `test_no_sm_gmtoffset_dependency` grep gate is the architectural test: it must pass.

    6. **Append `## Implementation status (Phase 12)` to spec** — same format. Confidence: Medium. Note the architectural distinction in the table notes column.

    7. Run pytest + compile scripts → all GREEN / exit 0.
  </action>
  <verify>
    <automated>cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py && cd .. && bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5" && bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime_no_autogmt.mq4" && grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md" && ! grep -q "import.*sm_gmtoffset\|from.*sm_gmtoffset" V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/test_sm_worktime_no_autogmt.py` → ≥3 PASSED, 0 failed
    - `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5"` → exit 0
    - `bash scripts/compile_mq.sh "resource_pack/MMM/SM Indicators/MT4/_helix_built/helpers/sm_WorkTime_no_autogmt.mq4"` → exit 0
    - `! grep -q "import.*sm_gmtoffset\|from.*sm_gmtoffset" V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py` → matches (NO sm_gmtoffset import — architectural gate per spec Section 8)
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py` contains `def compute_sm_worktime_no_autogmt(`
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py` contains `class SMWorkTimeNoAutoGmtParams`
    - File `V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py` contains `broker_gmt: int`
    - File `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5` contains `InpBrokerGMT`
    - `! grep -q "GlobalVariableGet.*sm_GMTOffset" "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5"` → matches (NO sm_GMTOffset GlobalVariable read in MQ5 either)
    - `grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md"` → matches
    - `python3 -c "from v3_intelligence.sm_indicators.helpers import compute_sm_worktime_no_autogmt; print('OK')"` (cwd V2) → prints OK
  </acceptance_criteria>
  <done>
    sm_WorkTime_no_autogmt implemented across MQ4 + MQ5 + Python; manual BrokerGMT input only — explicitly NO sm_gmtoffset dependency (verified by grep gate); spec footer appended; tests GREEN; compile clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: INDEX.md Implementation matrix — append Tier 0 row + run all-tier compile gate</name>
  <files>
    resource_pack/MMM/SM Indicators/docs/INDEX.md
  </files>
  <read_first>
    - resource_pack/MMM/SM Indicators/docs/INDEX.md (current state — Phase 11 left it ending at "Phase metadata"; Phase 12 appends a NEW H2 section after Phase metadata per D-14)
    - .planning/phases/12-sm-indicators-implementation/12-CONTEXT.md (D-14 INDEX matrix update at each tier's end)
    - .planning/phases/12-sm-indicators-implementation/12-RESEARCH.md (§ Spec Footer placement decision: new H2 sections appended at the bottom)
    - The 3 spec files just-edited (helpers/*.md) — confirms each carries `## Implementation status (Phase 12)` H2
  </read_first>
  <action>
    1. **Append `## Implementation matrix (Phase 12)` H2 section to `resource_pack/MMM/SM Indicators/docs/INDEX.md`** — Place AFTER existing "## Phase metadata" section. Exact markdown:
       ```markdown

       ---

       ## Implementation matrix (Phase 12)

       Per D-14, this section is updated at the end of each Phase 12 tier with
       the per-spec implementation status. Each row's "Status" cell summarizes
       the per-target footer added to the linked spec.

       ### Tier 0 — helpers (Plan 12-01)

       | # | Spec | MQ4 | MQ5 | Python | Confidence | Footer link |
       |---|------|-----|-----|--------|------------|-------------|
       | 1 | [sm_gmtoffset](./helpers/sm_gmtoffset.md) | ✅ | ✅ | ✅ | Medium | [#implementation-status-phase-12](./helpers/sm_gmtoffset.md#implementation-status-phase-12) |
       | 2 | [sm_WorkTime](./helpers/sm_WorkTime.md) | ✅ | ✅ | ✅ | Medium | [#implementation-status-phase-12](./helpers/sm_WorkTime.md#implementation-status-phase-12) |
       | 3 | [sm_WorkTime_no_autogmt](./helpers/sm_WorkTime_no_autogmt.md) | ✅ | ✅ | ✅ | Medium | [#implementation-status-phase-12](./helpers/sm_WorkTime_no_autogmt.md#implementation-status-phase-12) |

       Tier 0 review: approved YYYY-MM-DD (operator).

       ### Tier 1 — atomic indicators (Plan 12-02)

       *Pending — populated when Plan 12-02 lands.*

       ### Tier 2 — composite indicators (Plan 12-03)

       *Pending — populated when Plan 12-03 lands.*

       Status legend: ✅ Built (high/medium confidence) · ⚠ Built (low confidence per D-17) · ❌ Skipped (not applicable per D-18)
       ```

    1b. **Substitute date placeholders before commit (D-13 — date column has real value, not placeholder):**
       - In INDEX.md: replace `approved YYYY-MM-DD` with `approved $(date -I)` AT THE COMMIT TIME of Task 5 (Tier 0 review checkpoint approval). The shell-substitution runs in the executor's bash context: `sed -i "s/approved YYYY-MM-DD/approved $(date -I)/" "resource_pack/MMM/SM Indicators/docs/INDEX.md"`
       - In each of the 3 helper spec footers (sm_gmtoffset.md, sm_WorkTime.md, sm_WorkTime_no_autogmt.md): replace `2026-04-XX` with the actual implementation ISO date at commit time of that spec's indicator task: `sed -i "s/2026-04-XX/$(date -I)/g" "resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md"` (and same for the other two).
       - Acceptance gate at Task 5: `! grep -E "YYYY-MM-DD|2026-04-XX" "resource_pack/MMM/SM Indicators/docs/INDEX.md" "resource_pack/MMM/SM Indicators/docs/helpers/"*.md` — i.e., NO placeholder strings survive in committed evidence.

    2. **Run tier-batch compile gate**: `bash scripts/compile_mq_all_tier.sh tier0` → exit 0 (validates all 6 helper sources — 3 MQ4 + 3 MQ5 — compile clean as a batch).

    3. **Run full Phase 12 helper pytest suite**: `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/` → all GREEN.

    4. **Capture evidence** (if `.planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/` does not exist, create it):
       - Save `bash scripts/compile_mq_all_tier.sh tier0 2>&1 | tee .planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/tier0_compile.log`
       - Save `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/ > ../.planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/pytest_green.txt 2>&1 || true`
       - (Operator captures `mt5_chart_load.png` during user-review checkpoint — agent cannot drive Wine GUI per Phase 8.4 P04 SUMMARY constraint; explicitly NOT a blocker)
  </action>
  <verify>
    <automated>grep -q "## Implementation matrix (Phase 12)" "resource_pack/MMM/SM Indicators/docs/INDEX.md" && grep -q "Tier 0 — helpers (Plan 12-01)" "resource_pack/MMM/SM Indicators/docs/INDEX.md" && bash scripts/compile_mq_all_tier.sh tier0 && cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Implementation matrix (Phase 12)" "resource_pack/MMM/SM Indicators/docs/INDEX.md"` → matches
    - `grep -q "Tier 0 — helpers (Plan 12-01)" "resource_pack/MMM/SM Indicators/docs/INDEX.md"` → matches
    - `grep -c "✅" "resource_pack/MMM/SM Indicators/docs/INDEX.md"` → ≥9 (3 specs × 3 targets in Tier 0 table)
    - `bash scripts/compile_mq_all_tier.sh tier0` → exit 0
    - `cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/` → all PASSED, 0 failed
    - File `.planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/tier0_compile.log` exists
    - File `.planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/pytest_green.txt` exists
  </acceptance_criteria>
  <done>
    INDEX.md gains Implementation matrix with Tier 0 row populated. All 6 Tier 0 sources compile clean as a batch. Full helper pytest suite GREEN. Evidence captured for user review.
  </done>
</task>

<!-- ========================================================================= -->
<!-- WAVE 2 — Tier 0 review checkpoint (blocking)                                -->
<!-- ========================================================================= -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Tier 0 Review Checkpoint — operator approval gate before Plan 12-02</name>
  <what-built>
    3 helper indicators (sm_gmtoffset, sm_WorkTime, sm_WorkTime_no_autogmt) implemented across MQ4 + MQ5 + Python (9 source files), with Phase-12-wide compile script + pytest fixtures + .gitignore extension. Each spec has an `## Implementation status (Phase 12)` footer; INDEX.md has an `## Implementation matrix (Phase 12)` Tier 0 row. All compile clean (0/0) and Python tests are GREEN.
  </what-built>
  <how-to-verify>
    Operator reviews via the format from RESEARCH.md § Tier Review Checkpoint Format. Required steps:

    1. **Read evidence files:**
       - `cat .planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/tier0_compile.log` — confirm `Result: 0 errors, 0 warnings` for all 6 Tier 0 sources
       - `cat .planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/pytest_green.txt` — confirm `passed` count == total tests, `failed: 0`

    2. **Smoke-load in Wine MT5** (D-09):
       - Copy `.mq5` files to `~/.mt5/drive_c/Program Files/IC Markets KE MT5 Terminal/MQL5/Indicators/Helix_SM/helpers/`:
         ```bash
         mkdir -p "~/.mt5/drive_c/Program Files/IC Markets KE MT5 Terminal/MQL5/Indicators/Helix_SM/helpers"
         cp "resource_pack/MMM/SM Indicators/MT5/helpers/"*.mq5 \
            "~/.mt5/drive_c/Program Files/IC Markets KE MT5 Terminal/MQL5/Indicators/Helix_SM/helpers/"
         ```
       - In MT5 Navigator → Indicators → Helix_SM/helpers → drag each helper to a chart in this order: sm_gmtoffset (any chart), then sm_WorkTime (verify session boxes appear), then sm_WorkTime_no_autogmt (separate chart, set BrokerGMT manually)
       - Confirm Experts tab shows no `cannot allocate buffer` or runtime errors

    3. **(Optional) Smoke-load in Wine MT4** (D-08): operator-dependent — skip if no MT4 install accessible (per RESEARCH Open Question #2). Document state.

    4. **Capture screenshot** at `.planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/mt5_chart_load.png` showing Asia/London/US session boxes from sm_WorkTime on a EURUSD H1 chart.

    5. **Inspect generated source files** (spot-check 1-2 files):
       - `resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime.mq5` — confirm `GlobalVariableGet("sm_GMTOffset")` is present (validates dependency wiring)
       - `V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py` — confirm NO `sm_gmtoffset` import (validates architectural distinction)

    6. **Inspect spec footers**:
       - `tail -25 "resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md"` — confirm Implementation status table with paths and 3 ✅
       - Repeat for sm_WorkTime.md, sm_WorkTime_no_autogmt.md

    7. **Decide:**
       - "approved" → orchestrator commits the Tier 0 work and proceeds to Plan 12-02
       - "issues: <description>" → orchestrator routes back to /gsd:plan-phase 12 with --gaps to revise
  </how-to-verify>
  
  <files>n/a — checkpoint task; operator-driven verification only</files>
  <action>
    This is a checkpoint:human-verify task. The operator performs the verification described in <how-to-verify> above and replies per <resume-signal>.

    **On approval:** before committing the Tier 0 work, the executor runs the date-substitution per D-13 (no placeholder strings survive in committed evidence):

    ```bash
    APPROVAL_DATE=$(date -I)
    sed -i "s/approved YYYY-MM-DD/approved ${APPROVAL_DATE}/" "resource_pack/MMM/SM Indicators/docs/INDEX.md"
    sed -i "s/2026-04-XX/${APPROVAL_DATE}/g" "resource_pack/MMM/SM Indicators/docs/helpers/sm_gmtoffset.md"
    sed -i "s/2026-04-XX/${APPROVAL_DATE}/g" "resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime.md"
    sed -i "s/2026-04-XX/${APPROVAL_DATE}/g" "resource_pack/MMM/SM Indicators/docs/helpers/sm_WorkTime_no_autogmt.md"
    # Verify no placeholder survives:
    if grep -E "YYYY-MM-DD|2026-04-XX" "resource_pack/MMM/SM Indicators/docs/INDEX.md" "resource_pack/MMM/SM Indicators/docs/helpers/"*.md ; then
      echo "ERROR: date placeholders still present after substitution" >&2
      exit 1
    fi
    ```

    Then proceed with the standard Tier 0 commit and advance to Plan 12-02.

    On "issues: ..." reply: do NOT run the date substitution; route back to /gsd:plan-phase 12 with --gaps.
  </action>
  <verify>
    <automated>! grep -E "YYYY-MM-DD|2026-04-XX" "resource_pack/MMM/SM Indicators/docs/INDEX.md" "resource_pack/MMM/SM Indicators/docs/helpers/"*.md</automated>
  </verify>
  <done>Operator has typed "approved" (or described issues triggering gap-closure planning); on approval the date-substitution gate passes (no `YYYY-MM-DD` or `2026-04-XX` strings remain in INDEX.md or helper specs).</done>
  <resume-signal>Type "approved" to advance to Plan 12-02 (Tier 1 atomic indicators), or describe issues to trigger gap-closure planning.</resume-signal>
</task>


</tasks>

<verification>
**Phase-level checks for Plan 12-01 (run before tier review checkpoint):**

```bash
# 1. All Tier 0 sources compile clean
bash scripts/compile_mq_all_tier.sh tier0    # exit 0 expected

# 2. All Tier 0 Python tests GREEN
cd V2 && pytest -v tests/v3_intelligence/sm_indicators/helpers/    # 0 failed

# 3. All 3 specs carry the Implementation status footer
for f in helpers/sm_gmtoffset helpers/sm_WorkTime helpers/sm_WorkTime_no_autogmt; do
    grep -q "## Implementation status (Phase 12)" "resource_pack/MMM/SM Indicators/docs/${f}.md" || echo "MISSING: $f"
done

# 4. INDEX.md has the matrix
grep -q "## Implementation matrix (Phase 12)" "resource_pack/MMM/SM Indicators/docs/INDEX.md"

# 5. Architectural distinction enforced for no_autogmt variant
! grep -q "sm_gmtoffset" "V2/v3_intelligence/sm_indicators/helpers/sm_worktime_no_autogmt.py"
! grep -q "sm_GMTOffset" "resource_pack/MMM/SM Indicators/MT5/helpers/sm_WorkTime_no_autogmt.mq5"

# 6. Phase 8 / 8.4 / 8.5 fast-suite regression — must be unchanged
cd V2 && pytest -v tests/v3_intelligence/test_adr.py tests/v3_intelligence/test_pit.py tests/v3_intelligence/test_temporal_bucketing.py 2>&1 | tail -3
# Expected: same pass/fail counts as STATE.md baseline
```
</verification>

<success_criteria>
1. **Wave 0 RED scaffold landed:** scripts/compile_mq.sh + scripts/compile_mq_all_tier.sh exist and are executable; .gitignore has *.ex5 / *.ex4 entries with the !SM.Indicators exception; sm_indicators package skeleton + helpers subpackage exist; conftest.py exposes 5 fixtures; 3 RED test files for Tier 0 helpers exist
2. **Wave 1 implementation complete:** 3 helper indicators (sm_gmtoffset, sm_WorkTime, sm_WorkTime_no_autogmt) built across MQ4 + MQ5 + Python — 9 source files total — all compile-clean (0/0); 3 pytest files all GREEN; helpers/__init__.py re-exports all 3 compute_*() functions
3. **Spec footers appended:** all 3 helper specs have `## Implementation status (Phase 12)` H2 section with target/status/file/commit/date table per D-13
4. **INDEX matrix populated:** INDEX.md has `## Implementation matrix (Phase 12)` H2 with Tier 0 row per D-14
5. **Architectural distinction enforced:** sm_worktime_no_autogmt.py and sm_WorkTime_no_autogmt.mq5 contain NO reference to sm_gmtoffset (grep gates per spec Section 8)
6. **No regression:** Phase 8 / 8.4 / 8.5 fast-suite test counts unchanged from STATE.md baseline
7. **Evidence captured:** tier0_compile.log + pytest_green.txt under `.planning/phases/12-sm-indicators-implementation/evidence/tier0_compile_smoke/`
8. **Tier 0 review checkpoint approved:** operator types "approved" before Plan 12-02 begins
</success_criteria>

<output>
After completion, create `.planning/phases/12-sm-indicators-implementation/12-01-SUMMARY.md` documenting:
- Files created (Wave 0 scaffold + Wave 1 implementation + spec footers + INDEX matrix)
- Compile evidence (paths to logs)
- Test counts before/after
- Any [INFER]-tagged decisions made for spec ambiguities (e.g., default object prefixes, font choices)
- Rule deviations (if any) per Phase 8.4 P04 / 8.5 P01 precedent
- Tier 0 review approval timestamp + operator response
- Plan 12-02 ready signal
</output>
